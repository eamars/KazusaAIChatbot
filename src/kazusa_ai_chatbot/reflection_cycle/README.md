# Reflection Cycle Interface Control Document

## Document Control

- ICD id: `RC-ICD-001`
- Owning package: `kazusa_ai_chatbot.reflection_cycle`
- Interface boundary: conversation reads and reflection-run persistence through
  DB-interface modules, then optional memory promotion through
  `kazusa_ai_chatbot.memory_evolution`
- Runtime consumers: FastAPI lifespan worker, production CLI, deterministic
  tests, and future scheduler callers through public facades only
- Upstream owners: `kazusa_ai_chatbot.db.conversation_reflection` for
  conversation reads and `kazusa_ai_chatbot.db.reflection_cycle` for
  `character_reflection_runs`
- Downstream owner: `kazusa_ai_chatbot.memory_evolution` for durable shared
  memory mutations

This document is the production interface contract for Stage 1c reflection.
It supersedes the read-only-only contract while preserving the Stage 1a prompt
schemas for hourly and per-channel daily reflection.

## Purpose

The reflection cycle runs outside the live response path. It observes monitored
channels, stores inspectable reflection-run documents, creates per-channel
daily syntheses, and runs one global daily promotion pass that may write at
most one lore row and one self-guidance row through the evolving memory public
API.

Hourly and daily-channel reflection are evidence. They never write memory and
must not enter normal cognition directly. Normal chat may see only promoted
reflection memory, and only when `REFLECTION_CONTEXT_ENABLED` is true.

## Public Facades

Callers must import from `kazusa_ai_chatbot.reflection_cycle`:

```python
async def run_hourly_reflection_cycle(
    *,
    now: datetime | None = None,
    dry_run: bool,
) -> ReflectionWorkerResult: ...

async def run_daily_channel_reflection_cycle(
    *,
    character_local_date: str,
    dry_run: bool,
) -> ReflectionWorkerResult: ...

async def run_global_reflection_promotion(
    *,
    character_local_date: str,
    dry_run: bool,
    enable_memory_writes: bool,
) -> ReflectionPromotionResult: ...

def start_reflection_cycle_worker(
    *,
    is_primary_interaction_busy: Callable[[], bool],
) -> ReflectionWorkerHandle: ...

async def stop_reflection_cycle_worker(
    handle: ReflectionWorkerHandle,
) -> None: ...
```

`enable_memory_writes` is required and has no default. CLI and service code
must not import worker, repository, prompt, or DB internals.

The worker-owned promotion path passes its busy probe into the internal
promotion runner so a chat turn that appears after the LLM call can defer
memory writes. CLI and manual promotion calls use the package facade above.

## DB Boundaries

Conversation history reads belong to:

```text
kazusa_ai_chatbot.db.conversation_reflection
```

Reflection-run persistence belongs to:

```text
kazusa_ai_chatbot.db.reflection_cycle
```

The reflection package must not open database clients, operate on Motor or
PyMongo collection handles, or express raw MongoDB commands. The repository
imports `db.reflection_cycle` as its storage interface and calls only named
functions.

`character_reflection_runs` documents use `_id=run_id` and also store
`run_id`. Required indexes:

```text
reflection_run_id_unique:
  [("run_id", 1)], unique

reflection_kind_date_status:
  [("run_kind", 1), ("character_local_date", 1), ("status", 1)]

reflection_scope_hour:
  [("scope.scope_ref", 1), ("hour_start", 1)]

reflection_source_run_ids:
  [("source_reflection_run_ids", 1)]
```

## Run Documents

Run kinds:

- `hourly_slot`: stores one message-bearing UTC hour for one monitored channel.
- `daily_channel`: stores one per-channel synthesis for a character-local day.
- `daily_global_promotion`: stores the global daily promotion prompt output and
  promotion decisions.

`daily_global_promotion` uses a synthetic system scope:
`scope_ref="daily_global"`, `platform="system"`,
`platform_channel_id="global"`, and `channel_type="system"`.

Statuses:

- `succeeded`
- `failed`
- `skipped`
- `dry_run`

All run documents include prompt version, attempt count, raw scope triple,
source message refs or source reflection run ids, validation warnings, output,
and error text when relevant. Source message refs are persistence metadata and
are not generated by the LLM.

## Prompt Contracts

Stage 1a hourly and daily-channel prompt schemas remain unchanged. They keep
English JSON keys and enum values, Simplified Chinese generated free text, and
bounded prompt-safe message projection.

Global promotion lives in `promotion.py` as a separate LLM block:

```text
GLOBAL_PROMOTION_PROMPT_VERSION
GLOBAL_PROMOTION_SYSTEM_PROMPT
_global_promotion_llm
run_global_promotion_llm
```

The promotion prompt consumes compact `daily_channel` cards and bounded
evidence cards. It outputs `promotion_decisions` only. Deterministic validators
and similarity resolution decide whether memory APIs are called.

## Memory Boundary

Reflection promotion may call only these public memory APIs:

- `find_active_memory_units`
- `insert_memory_unit`
- `supersede_memory_unit`
- `merge_memory_units`

Promotion constructs `EvolvingMemoryDoc` directly with:

- `authority="reflection_promoted"`
- `source_kind="reflection_inferred"`
- empty `source_global_user_id`
- `memory_type="fact"` for `lore`
- `memory_type="defense_rule"` for `self_guidance`
- evidence refs and privacy review populated before the memory call

The legacy memory compatibility path is not part of this interface.

Similarity decisions must use returned score tuples from
`find_active_memory_units`. If scores are missing, malformed, or unavailable,
promotion records a skipped/deferred result and performs no memory mutation.

## Worker Schedule

FastAPI lifespan owns exactly one worker task when
`REFLECTION_CYCLE_DISABLED` is false. The service passes a busy-probe callback;
the worker never imports service state.

Default schedule:

- `REFLECTION_WORKER_INTERVAL_SECONDS=900`
- `REFLECTION_HOURLY_SLOTS_PER_TICK=3`
- `REFLECTION_DAILY_RUN_AFTER_LOCAL_TIME=04:30`
- `REFLECTION_PROMOTION_RUN_AFTER_LOCAL_TIME=05:00`

Gates use `CHARACTER_TIME_ZONE`. Stage 1a hourly slots remain UTC, while daily
grouping uses the character-local date of each hourly slot.

Tick priority:

1. Skip if primary chat or post-dialog work is busy.
2. Run due hourly slots.
3. Run due daily-channel syntheses after terminal hourly documents exist.
4. Run daily global promotion after daily-channel documents are terminal.
5. Re-check the busy probe before starting each next unit of work.

Terminal hourly statuses are `succeeded`, `failed`, `skipped`, and `dry_run`;
a failed hourly slot does not block daily synthesis forever.
If non-terminal hourly documents are present, daily synthesis uses the terminal
subset and records a `partial_hourly_input` validation warning on the daily run
document.

## Feature Flags

- `REFLECTION_CYCLE_DISABLED=false`: worker runs by default.
- `REFLECTION_CONTEXT_ENABLED=false`: promoted context is off by default.
- `REFLECTION_LORE_PROMOTION_ENABLED=true`: lore lane is enabled by default.
- `REFLECTION_SELF_GUIDANCE_PROMOTION_ENABLED=true`: self-guidance lane is
  enabled by default.

Feature flags are process-loaded from config. Changing them requires a process
restart unless a test monkeypatches the module value directly.

All Stage 1c LLM calls use the existing consolidation route:

- `CONSOLIDATION_LLM_BASE_URL`
- `CONSOLIDATION_LLM_API_KEY`
- `CONSOLIDATION_LLM_MODEL`

No reflection-specific LLM route exists.

## Prompt-Facing Context

`context.py` exposes compact promoted reflection context only when enabled.
Allowed shape:

```python
{
    "promoted_lore": [dict],
    "promoted_self_guidance": [dict],
    "source_dates": [str],
    "retrieval_notes": [str],
}
```

Context rows come from active Stage 1b memory units with
`source_kind="reflection_inferred"` and the lane memory type. The projection
caps each lane before cognition. Raw hourly outputs, raw transcripts, full run
documents, unpromoted candidates, source message refs, user ids, and private
details are forbidden.

## Logging

`INFO` logs show lifecycle and operator-critical outcomes: worker start/stop,
worker pass summaries, disabled state, no-write outcomes, and actual promoted
memory names/content previews.

`DEBUG` logs show supporting diagnostics: prompt character counts, confidence,
validation warnings, boundary/privacy summaries, score values, source run ids,
and evidence-card ids.

No log level may include raw transcripts, raw hourly output, full run
documents, source message refs, user identifiers, display names, or unsanitized
private details.

## CLI

```powershell
python src\scripts\run_reflection_cycle.py hourly --dry-run
python src\scripts\run_reflection_cycle.py daily --dry-run
python src\scripts\run_reflection_cycle.py promote --dry-run
```

`daily` and `promote` default to the previous character-local date. Use
`--character-local-date YYYY-MM-DD` for deterministic runs.

## Forbidden Couplings

Reflection must not:

- call the chat endpoint or send autonomous messages;
- use scheduled-event persistence;
- import service internals;
- update user profiles, user memory units, character state, or conversation
  progress;
- write memory except through the memory-evolution public APIs;
- expose raw hourly reflection as live cognition context.

## Verification

Required checks before sign-off:

- focused repository, worker, promotion, context, service, and integration
  tests pass;
- source greps prove Stage 1c writers avoid the legacy memory compatibility
  path;
- source greps prove reflection package and CLI avoid raw database operations;
- global promotion live LLM cases are run one by one when the configured local
  LLM endpoint is available, and artifacts are inspected against the mini-gate;
- dry-run CLI commands complete or record an environment-specific blocker.
