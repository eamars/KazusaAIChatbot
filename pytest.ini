[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = src
markers =
    live_llm: requires a running LM Studio instance
    live_db: requires a running MongoDB instance (deselected by default)
addopts = -m "not live_db and not live_llm" -rA
log_cli = true
log_cli_level = INFO
