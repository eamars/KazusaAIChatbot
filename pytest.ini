[pytest]
asyncio_mode = auto
testpaths = tests
pythonpath = src
markers =
    live_llm: requires a running LM Studio instance (deselected by default)
    live_db: requires a running MongoDB instance (deselected by default)
addopts = -m "not live_llm and not live_db"
