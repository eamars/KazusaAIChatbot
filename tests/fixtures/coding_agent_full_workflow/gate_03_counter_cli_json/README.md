# Counter CLI

This package counts lines in a text file and prints a plain text summary.
The gate asks the coding agent to add JSON output to runtime source while
keeping tests unchanged during the proposal revision.
