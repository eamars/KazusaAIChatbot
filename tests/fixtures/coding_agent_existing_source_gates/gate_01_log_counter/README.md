# Log Counter

Count severity-prefixed application log lines.

```powershell
python log_counter.py app.log
```

The command prints one count per severity and reports how many malformed lines
were skipped.
