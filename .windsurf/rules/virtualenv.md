---
trigger: always_on
---

When working with Python projects, always use a virtual environment. 

Look for the `.venv` directory in the project root. If it exists, activate it with `.venv\Scripts\activate` on Windows or `source .venv/bin/activate` on Unix-like systems.

If it doesn't exist, create one with `python -m venv .venv` and activate it.
