# Multi Source Cognition Stage 03 Inactive Prompt Variant Notes

Status: reference only.

This note records inactive future prompt-variant directions that were discussed
during Stage 03. It is not a runtime contract, contains no production prompt
text, and must not be imported by source code.

## Inactive Future Directions

`reflection_signal` with `reflection_artifact`:

- A future approved plan may define a cognition prompt variant for promoted,
  gated reflection artifacts.
- Stage 03 does not enable this trigger source, input source, selector variant,
  prompt key, or handler path.
- This note does not authorize reflection-triggered cognition work.

`internal_thought` with `internal_monologue`:

- A future approved plan may define a cognition prompt variant for internally
  generated monologue events.
- Stage 03 does not enable this trigger source, input source, selector variant,
  prompt key, or handler path.
- This note does not authorize internal-thought cognition work.

## Boundaries

- Do not add these labels to Stage 03 runtime prompt selection.
- Do not add prompt templates, examples, output schemas, or LLM stages from this
  note.
- Do not route RAG, consolidation, scheduler, adapter, persistence, or database
  behavior from this note.
- Do not treat this reference note as approval for Stage 07, Stage 08, or any
  other future implementation stage.
