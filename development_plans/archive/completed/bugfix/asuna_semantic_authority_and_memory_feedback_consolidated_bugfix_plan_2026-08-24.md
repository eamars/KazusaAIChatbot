# Asuna Semantic Authority And Memory Feedback Consolidated Bugfix Plan

## Summary

- Goal: stop character-authored or memory-amplified interaction tactics from
  becoming the current user's hidden intent, obligation, permission, or
  mandatory response payoff while preserving Asuna's learned personality,
  multi-emotion system, initiative, and hybrid agentic flow.
- Status: closed_with_residual_failures; execution was terminated and archived
  on 2026-08-25 under the user's mandatory single-final-attempt closure rule.
- Scope boundary: semantic progression, Conversation Progress ownership,
  user-memory commitment admission, learned-memory scope certification, typed
  retrieval authority, recipient applicability, Simplified-Chinese runtime
  prompt consistency across the consolidation stages changed by this plan,
  ephemeral response-content provider authority, targeted read-only audit, and
  exact-manifest remediation.
- Change direction: one canonical authority chain from current observation to
  visible response and from settled response to durable memory; LLM stages own
  meaning, deterministic code owns typed admission, scope, limits,
  persistence, and lifecycle.
- Acceptance state: execution is complete and the plan lifecycle is closed.
  Functional sign-off was not achieved; the immutable final attempt and every
  residual failure are recorded in `Final Closure Record` below.
- Plan-scoped fixed execution constraint: one GPT-5.6 Luna subagent at maximum
  reasoning and standard normal speed performs production implementation and
  test execution. The user may change this constraint.
- Architecture and acceptance owner: the parent agent.
- Database mutation authority: on 2026-08-24 the user identified Asuna's
  database as a test database and categorically authorized removal of every
  active legacy learned-memory row that fails the canonical W3 typed
  certificate. W5 still emits exact IDs, hashes, reasons, and backup material;
  W6 may lifecycle-reject the unchanged manifest without a second per-ID
  approval. Any row drift still stops the apply.

This plan supersedes the active planning roles of:

- `multi_turn_semantic_progression_and_response_goal_fixation_bugfix_plan_2026-08-23.md`;
- `unaccepted_character_proposal_active_commitment_lane_authority_bugfix_plan_2026-08-24.md`;
- `cross_user_character_memory_scope_and_authority_bugfix_plan_2026-08-23.md`.

Their RCA and execution evidence remain preserved in the superseded archive.
This is the sole ongoing successor for those three plans. The independently
owned Agentic Resolver readiness plan and the epistemic/role-provenance plan
remain active and unchanged by this consolidation.

## Problem And Evidence

The current failure mode is semantic authority capture, expressed through one
self-reinforcing loop:

```text
learned character tendency
  -> character uses a visible tactic
  -> Conversation Progress overstates the tactic as a decision constraint
  -> cognition reselects it despite a changed current user request
  -> L3/dialog faithfully repeat the payoff
  -> consolidation can misstore the character demand as a user agreement
  -> reflection can promote repeated behavior as global character authority
  -> later users and later turns receive stronger pressure
```

Two current evidence sets establish the system boundary:

1. Latest-anchored QQ group `480386272` evidence shows distinct participant
   IDs and correct triggering-user binding. The defect occurs later, when a
   relationship, consent, promise, permission, or intimate role addressed to
   one participant is treated as applicable to another participant.
2. The fresh memory-enabled L2 replay shows Asuna answering a changed task and
   then returning to her unanswered reward proposal. Conversation Progress
   marked that proposal `decision_critical`; consolidation then wrote the false
   `active_commitment`:

```text
用户同意在获得一之濑明日奈的决策协助后，提供一份具体的奖励作为交换。
```

The user made no such agreement.

The exact current RCAs are:

- `test_artifacts/diagnostics/asuna_group_480386272_memory_personality_policy_rca_20260823.md`;
- `test_artifacts/reviews/asuna_multi_turn_semantic_attractor_system_rca_20260823.md`;
- `test_artifacts/reviews/asuna_exchange_fixation_l2_replay_system_rca_20260824.md`.

## Confirmed Decisions

1. Memory-driven character evolution is intentional. Memory may shape
   motivation, stance, voice, and tactics.
2. Memory evidence is not current user intent, consent, permission,
   commitment, relationship ownership, or a mandatory visible response goal.
3. QQ/platform identity binding is mechanically correct in the captured
   incident. Recipient applicability after identity binding is defective.
4. Current accepted user evidence owns the current semantic delta. Historical
   continuity and character tendency remain supporting or conditional inputs.
5. A character-authored proposal may remain factual scene continuity. Until
   the current user responds to it, it is not the user's goal, blocker,
   acceptance, obligation, or decision constraint.
6. Continuing the underlying practical task does not accept or reopen a prior
   character-authored offer, demand, bargain, condition, or relational payoff.
7. `active_commitment` is character-owned future behavior accepted for the
   current user. It does not represent a debt or promise imposed by the
   character on the user.
8. Public group visibility proves what was said, by whom, and to whom. It does
   not transfer one participant's permission or relationship to another.
9. Repeated character behavior may qualify as global lore or conditional
   self-guidance only after target-removal, privacy, identity, and scope review.
10. Whole-database erasure is rejected as the repair. The data path is a
    read-only audit followed by a separately approved exact lifecycle manifest.
11. The live response path remains one A1, one A2, one G, one P, one content
    plan, and one dialog call. No semantic evaluator, retry loop, response
    suppressor, or foreground database read is added.
12. The current multi-emotion derivation, state, projection, order, causes,
    limits, reducers, and persistence contracts remain unchanged.
13. The existing resolver-readiness evaluation and the broader unsupported
    autobiography/temporary-role work remain under their independent active
    plans and ownership.
14. Runtime LLM prompt prose in `consolidation/` and the reflection-promotion
    stage changed by this plan uses native Simplified Chinese. Contractual JSON
    keys, enum values, IDs, URLs, code, model labels, and source quotations
    retain their exact source form.
15. S1G proves that the former model-visible key
    `selection_owner_role` is itself an ambiguous semantic affordance for the
    configured local model. The canonical current-turn contract replaces it
    big-bang with `response_content_provider_role`; the meaning remains who
    supplies input-unspecified reply content. This metadata is ephemeral and
    absent from persisted episode/progress state, so the cutover needs no data
    migration or compatibility alias.

## Scope And Change Direction

### Must Do

1. Retain and reverify the already implemented bounded `overused_moves`
   projection, current-delta G/P guidance, correction authority, L3 semantic
   fidelity, and dialog semantic fidelity.
2. Correct Conversation Progress ownership so unanswered character proposals
   remain scene/history evidence rather than user-owned decision state.
3. Carry the lane router's accepted user-memory versus active-commitment
   contract into the existing extractor and deterministic validator.
4. Prevent character demands, user silence, and underlying-task continuation
   from becoming fabricated active commitments.
5. Require independent global-scope certification for learned character
   self-guidance and reflection promotion.
6. Preserve typed learned-memory metadata through normal retrieval and
   first-cycle prewarm, then map facts and conditional guidance to their
   correct cognition authority lanes.
7. Add one compact recipient-applicability rule to existing A2/G/P guidance.
8. Add read-only scope audit and drift-safe exact-manifest repair tooling using
   existing public database and memory-lifecycle operations.
9. Run deterministic owner tests and every required real-LLM case one at a
   time, inspect raw evidence, and preserve the original multi-emotion design.
10. Rewrite every runtime LLM prompt owned by `consolidation/`, plus the
    reflection-promotion prompts changed by W3, into native Simplified Chinese
    without changing schemas, semantic ownership, stage count, or call budget.
11. Complete an independent read-only implementation review before user
    sign-off.
12. Replace the ambiguous response-operation ownership key through one
    canonical current-turn contract, keep A2 existential appraisal owned by
    the current character, and make G carrier provenance separate character-
    side motive from user-side relationship facts.

### External Active Plans Outside This Ownership

- Standalone Agentic Resolver Phase 2 readiness evaluation.
- Epistemic and role-provenance fidelity work produced by the separate Asuna
  50-turn conversation-practice plan.

This plan changes neither plan's scope, status, execution authority, evidence,
or lifecycle.

### Deferred

- Relationship-axis formulas, perceived-closeness calibration, character seed
  profile changes, or identity-ledger redesign.
- QQ adapter parsing, platform identity migration, message ordering, delivery,
  scheduler behavior, or response-ratio tuning.
- New memory collections, new unit types, a user-obligation lane, persisted
  group transcript packets, or a second character-identity path.
- Automatic rewriting, redaction, rerouting, or re-homing of historical memory
  rows.
- Any database apply before the user approves the exact generated manifest.

## Mandatory Skills

- `development-plan`: lifecycle, handoff, change gates, evidence, and closure.
- `local-llm-architecture`: prompt minimality, weak-model contracts, and call
  budget preservation.
- `no-prepost-user-input`: LLM-owned acceptance and commitment semantics.
- `py-style`: every Python source or test edit.
- `cjk-safety`: every Python edit containing CJK prompt text.
- `test-style-and-execution`: deterministic versus patched versus real-LLM
  coverage and one-at-a-time live execution.
- `character-test`: memory-enabled service conversations and adaptive quality
  inspection.
- `llm-trace-debug`: protected prompt, stage, and output evidence.
- `database-data-pull`: read-only diagnostic and audit exports.

## Mandatory Rules

- Use `venv\Scripts\python.exe` for Python and pytest commands.
- Read no `.env` content. Existing service and test entrypoints may load their
  configured environment normally.
- Use the configured real production-model routes. The intentionally removed
  debug LLM remains absent.
- Preserve original user and model messages without translation in evidence.
- Run real-LLM cases individually and inspect each result before proceeding.
- Use `parse_llm_json_output(...)` as the canonical structured-output parser.
- Treat off-contract enum/type output as structural invalidity and keep it out
  of persistence. Deterministic code must not infer, rewrite, or reroute its
  semantic meaning.
- Every runtime prompt change must express a reusable semantic ownership
  invariant. Captured-case dialog, nouns, sequence, expected output,
  reward/exchange framing, test wording, close paraphrases, and test-shaped
  examples are forbidden. Tests must evaluate the behavior contract
  independently of prompt wording; scenario coupling fails acceptance even
  when pytest passes.
- Preserve concurrent and user-authored working-tree changes.
- Any production path outside the declared change surface requires parent
  justification, a plan amendment, and explicit user approval before editing.
- The parent chooses ordinary live-test messages under the user's delegated
  discretion and asks the user only for genuinely ambiguous semantic quality
  judgments.

## Target State

### 1. Live semantic authority

The live path keeps its existing stages:

```text
current observation
  -> A1 observation/appraisal question
  -> A2 character judgment with bounded evidence
  -> G one current response goal
  -> P one coherent response plan
  -> L3 one bounded content plan
  -> dialog final wording
```

Authority is interpreted as follows:

- current user input and explicit current-user corrections own the current
  semantic delta;
- direct facts establish only facts within their declared scope;
- participant continuity establishes prior actors, actions, outcomes, and
  current scene history;
- conditional character guidance shapes tactics, motivation, and boundaries;
- personality, relationship, and affect shape judgment and voice after the
  current objective is fixed;
- none of the supporting lanes independently establishes current user intent,
  consent, permission, commitment, or required payoff.

`overused_moves` remains an LLM-authored observation of visible response
maneuvers that already occurred. Deterministic code copies at most four rows,
in order, at most 120 characters each. A1 does not receive the list. A2, G, P,
and L3 receive the exact same bounded list. The list is neither a prohibition
nor a future-response instruction.

G selects a primary contribution to the latest observation. A prior tactic may
be selected again when the user explicitly accepts, rejects, responds to,
references, asks about, materially changes, or reopens it. P, L3, and dialog
preserve that selected meaning and may not add an unselected relational or
contractual payoff.

### 2. Conversation Progress ownership

The existing scene and event calls, output shapes, relevance enum, persistence
schema, route, caps, and source references remain unchanged.

The scene observer follows these fixed semantics:

- `user_goal` and `current_blocker` require user-owned evidence;
- a character-authored unanswered offer, demand, bargain, condition, or
  relational payoff may appear in factual narrative/current-thread context;
- that proposal does not become user acceptance, obligation, goal, or blocker;
- an explicit current-user response may make it part of user-owned current
  state.

The event observer follows these fixed semantics:

- preserve the exact actor, action, object, beneficiary, precondition, and
  lifecycle evidence;
- an unanswered character-authored proposal has `scene` or `history`
  relevance;
- it has `decision` relevance only when the current user responds to, accepts,
  rejects, references, asks about, materially changes, or explicitly reopens
  it;
- continuing the underlying task alone is not such a response.

### 3. User-memory and active-commitment admission

The existing lane-router LLM remains the coarse semantic owner. The existing
single extractor call receives one code-owned dynamic contract:

```json
{
  "memory_unit_write_contract": {
    "enabled_lanes": ["user_memory_units"],
    "allowed_unit_types": [
      "stable_pattern",
      "recent_shift",
      "objective_fact",
      "milestone"
    ]
  }
}
```

The exact mapping is:

| Accepted lane | Allowed candidate types |
|---|---|
| `user_memory_units` | `stable_pattern`, `recent_shift`, `objective_fact`, `milestone` |
| `active_commitment` | `active_commitment` |
| both | the ordered union of both rows |
| neither | no extractor persistence work |

The extractor prompt defines `active_commitment` as future behavior accepted
by the character for the current user. It explicitly treats character demands,
user silence, and underlying-task continuation as insufficient. It returns an
empty list when no allowed durable meaning exists.

The deterministic validator receives the same allowed set. A candidate type
outside the set is a structural contract error: the candidate is logged and
dropped without semantic rewriting. Valid independent candidates remain
eligible. The one-attempt extractor budget and model-call count remain
unchanged.

### 4. Learned character-memory scope certification

The coarse consolidation router keeps its current output shape and receives
one target-removal decision rule: remove the source user, addressee,
relationship target, and private scene; if the accepted behavior is no longer
accurate and appropriate for the character with people generally, it is not
global character self-guidance.

The existing self-guidance specialist and reviewer independently emit:

```json
{
  "global_applicability": "global | scoped | absent",
  "target_specific_meaning_removed": true,
  "affects_identity_or_boundaries": false,
  "private_detail_risk": "low | medium | high",
  "user_details_removed": true,
  "reason": "bounded semantic justification"
}
```

Persistence admits a learned global self-guidance write only when both stages
agree on `global`, both confirm target removal, both exclude identity/boundary
ownership, and the final review reports low private-detail risk with user
details removed. Disagreement and structural failure produce no write. Code
does not choose another semantic lane.

`MemoryPrivacyReview` gains the exact three scope fields
`global_applicability`, `target_specific_meaning_removed`, and
`affects_identity_or_boundaries` for new learned-character writes. Seed and
curated knowledge retain their existing contracts.

Daily reflection promotion uses the same certificate. One independent
reviewer receives the promoter's one or two proposed writes in one candidate-
gated background call. It accepts or rejects exact candidates and cannot
rewrite them. Missing privacy evidence remains unreviewed. Identity/boundary
candidates remain owned by the existing identity-growth pipeline. This adds at
most one background call on promotion runs that propose a write and adds no
live-chat latency.

### 4A. Consolidation prompt language contract

All runtime system-prompt prose in `src/kazusa_ai_chatbot/consolidation/` and
the W3 reflection-promotion promoter/reviewer uses native Simplified Chinese.
This includes the lane router, user-memory extractor, merge judge, rewrite
stage, stability judge, character-self-guidance specialist/reviewer, and
reflection promoter/reviewer. Retired prompt constants are audited and remain
Chinese; they do not re-enter runtime execution.

The rewrite is semantic localization, not literal sentence substitution. It
preserves stage ownership, modality, negation, evidence boundaries, output
shape, field meaning, and call count. JSON keys, enum values, identifiers,
placeholders, URLs, code, commands, model labels, and source quotations remain
byte-exact where contractual. Newly generated free-text requirements stay
Simplified Chinese. No bilingual repetition or explanatory translation is
added.

Translation tests inspect the prompt constants independently from behavioral
fixtures: they reject remaining non-contractual English prose, preserve the
contract token allowlist, and reject captured-case vocabulary. Each distinct
LLM stage retains a one-at-a-time real-LLM positive or negative control so
translation loss cannot be hidden by deterministic prompt-string tests.

### 5. Typed retrieval and recipient applicability

Normal shared-memory projection and first-cycle prewarm preserve and partition
these fields before formatting:

- `memory_type`;
- `scope_type` and `scope_global_user_id`;
- `source_kind` and `source_system`;
- `authority`, `truth_status`, and `origin`;
- scope/privacy review fields;
- repository-owned source references for trace only.

The deterministic cognition connector maps typed metadata:

| Stored evidence | Cognition lane |
|---|---|
| exact current-user continuity | `participant_continuity` |
| `defense_rule` / character self-guidance | `conditional_character_guidance` |
| validated global `fact` or curated lore | `character_world_context` |
| mixed, unsupported, or incomplete learned metadata | excluded with diagnostic evidence |

A2, G, and P receive one compact rule: a public scene proves what was said, by
whom, and to whom; consent, permission, promise, relationship, or role
addressed to one participant does not transfer to another participant merely
because both can see it. A1 remains current-observation focused.

### 6. Targeted data audit and remediation

The read-only audit uses existing public `db.script_operations` exports and
the memory-evolution lifecycle API. It exports active learned global `fact` and
`defense_rule` rows from conversation-accepted and reflection-promoted sources
with exact IDs, hashes, authority, provenance, refs, and privacy metadata.

An offline LLM audit applies the same scope certificate and emits raw JSON.
The readable review separates:

- valid target-free global fact;
- valid target-free conditional self-guidance;
- scoped/private/relationship material;
- identity-or-boundary material;
- unresolved rows requiring user judgment.

The active `reflection_e0c29b7a8e2be3bff65b64640a719495` row named
`交换条件互动模式` is an explicit audit seed with no predetermined disposition.

The apply manifest contains exact memory-unit IDs, expected content hashes,
current statuses, reasons, and `reject` dispositions. The categorical test-DB
authority covers every active learned row whose authority/source pair is
learned and whose W3 certificate is absent, incomplete, scoped, target-bearing,
identity/boundary-affecting, privacy-unsafe, or otherwise invalid. The repair
tool backs up and re-fetches each row, stops on drift, calls
`memory_evolution.reject_memory_unit(...)`, verifies cache invalidation, and
re-audits. Certified rows remain unchanged. Historical rows are not rewritten
or automatically re-homed.

## Runtime And Resource Constraints

- Foreground cognition: exactly A1, A2, G, P.
- Foreground content/dialog: exactly one content-plan and one dialog call.
- Conversation Progress: unchanged one scene and one event call.
- Memory extraction: unchanged one candidate-gated extractor call.
- Consolidation specialist/reviewer roster: unchanged.
- Daily reflection: at most one new batched reviewer call when write candidates
  exist.
- Foreground database reads, resolver calls, agents, retries, and tools:
  unchanged.
- Persistent collections, indexes, user-memory unit types, and Conversation
  Progress schemas: unchanged.
- The ephemeral six-field `response_operation` contract changes one key
  big-bang from `selection_owner_role` to
  `response_content_provider_role`; its role enum, selection flag, embedded
  actor/target direction, call topology, and persistence boundaries remain
  unchanged. The old key is rejected rather than aliased.
- Audit/remediation: offline only; never runtime middleware.
- Existing prompt and payload caps remain enforced. New prompt content must fit
  without removing or reordering affect, cause, participant, or continuation
  evidence.

## Cutover Policy

Overall strategy: bigbang for code contracts.

| Area | Policy | Instruction |
|---|---|---|
| Live semantic prompts and projections | bigbang | One canonical authority contract; retain no old-path aliases or fallbacks. |
| Ephemeral response-operation carrier | bigbang | Rename the ambiguous provider key in the decontextualizer, typed episode contract, cognition projection, tests, and ICD together; reject the old key and add no stored-data migration. |
| Conversation Progress semantics | bigbang | Replace the ambiguous proposal relevance semantics in the existing calls. |
| Memory extractor lane contract | bigbang | Require the router-approved type set in the one canonical extractor path. |
| Learned-memory write/retrieval authority | bigbang | New learned writes and reads use the certified typed contract immediately. |
| Existing database rows | migration | Read-only exact manifest, categorical test-DB authority, backup, drift check, lifecycle rejection, and re-audit. |
| Tests | bigbang | Exact owner and live gates target only the canonical contracts. |
| Planning records | bigbang | This plan is the sole active successor to its three owned source plans; independently owned active plans remain unchanged. |

## Change Surface

### Modify

- `src/kazusa_ai_chatbot/conversation_progress/recorder.py`
  - preserve implemented repeated-move observation;
  - add character-proposal versus user-owned scene/event authority.
- `src/kazusa_ai_chatbot/conversation_progress/projection.py`
- `src/kazusa_ai_chatbot/conversation_progress/policy.py`
- `src/kazusa_ai_chatbot/conversation_progress/__init__.py`
  - retain the implemented exact bounded `overused_moves` public projection.
- `src/kazusa_ai_chatbot/nodes/persona_supervisor2_cognition.py`
  - retain bounded move input;
  - map typed learned memory to the correct cognition authority lane;
  - require the same canonical classifier for RAG and bounded promoted-
    reflection context ingress.
- `src/kazusa_ai_chatbot/cognition_episode.py`
  - replace `selection_owner_role` with the canonical
    `response_content_provider_role` key in the exact ephemeral six-field
    contract, validator, and selected-operation equality guard;
  - reject the retired key without an alias, fallback, or migration path.
- `src/kazusa_ai_chatbot/nodes/persona_supervisor2_msg_decontextualizer.py`
  - emit the new provider key and describe its role positively as supplying
    input-unspecified reply content;
  - preserve `selection_required`, response direction, embedded actor/target
    direction, one model call, and exact validation.
- `src/kazusa_ai_chatbot/cognition_core_v3/facade.py`
  - retain the canonical bounded move input and unchanged state transaction;
  - consume the same Chinese current-observation/direct-fact authority rule as
    the A1 packet so the higher-priority stage system prompt cannot contradict
    it;
  - consume the same background-context/current-goal authority rule as G and P
    packets so higher-priority instructions cannot promote evidence into final
    stance.
- `src/kazusa_ai_chatbot/cognition_core_v3/prompt.py`
  - retain current-delta/correction guidance;
  - add recipient applicability to A2/G/P;
  - keep background memory and relationship interpretation in their typed
    interpretation, monologue, willingness, and expression roles unless the
    current observation makes them part of the current response objective;
  - consume the new response-content provider key as a reply-content fact,
    keep `existential_drive` about the current character rather than the
    user's ability or agency state, and give G's reason/cause/monologue fields
    explicit character-side versus user-fact provenance;
  - preserve every affect, entity, and continuation projection.
- `src/kazusa_ai_chatbot/cognition_shared/transition_guards.py`
  - preserve the existing knowledge-gap FSM and strict terminal-state guard;
  - require a negative uncertainty request to produce an actual accepted
    decrease before it can trigger the reduced or resolved transition;
  - keep zero-clamped shifts as valid no-change receipts so one resolver
    recurrence cannot terminalize and then recreate the same evidence-scoped
    canonical root.
- `src/kazusa_ai_chatbot/cognition_shared/contracts.py`
- `src/kazusa_ai_chatbot/cognition_shared/surface.py`
- `src/kazusa_ai_chatbot/cognition_shared/surface_stages.py`
- `src/kazusa_ai_chatbot/nodes/persona_supervisor2_l3_surface.py`
- `src/kazusa_ai_chatbot/nodes/dialog_agent.py`
  - retain the implemented canonical move handoff and selected-goal fidelity.
- `src/kazusa_ai_chatbot/cognition_core_v3/README.md`
- `src/kazusa_ai_chatbot/nodes/README.md`
  - document the canonical provider key, character-subject appraisal, and
    absence of compatibility aliases.
- `src/kazusa_ai_chatbot/consolidation/memory_units.py`
  - add the router-approved write contract, prompt semantics, and off-contract
    type validation;
  - localize extractor, merge-judge, rewrite, and stability prompt prose into
    native Simplified Chinese.
- `src/kazusa_ai_chatbot/consolidation/lane_router.py`
  - add the generic global/user/group/transient target-removal distinction to
    the existing coarse semantic contract;
  - localize the remaining runtime prompt prose into native Simplified Chinese.
- `src/kazusa_ai_chatbot/consolidation/character_self_guidance.py`
  - require independent scope certificates and persist the actual review;
  - localize specialist and reviewer prompts into native Simplified Chinese.
- `src/kazusa_ai_chatbot/memory_evolution/models.py`
  - add the three learned-memory scope fields.
- `src/kazusa_ai_chatbot/reflection_cycle/promotion.py`
  - preserve source privacy, add one batched scope reviewer, and fail closed on
    scope disagreement;
  - keep the complete promoter/reviewer prompt chain in native Simplified
    Chinese.
- `src/kazusa_ai_chatbot/reflection_cycle/context.py`
  - preserve the existing typed authority, stable identity, active status, and
    complete privacy certificate from repository rows before cognition;
  - add no semantic inference, database query, model call, retry, or prompt
    prose.
- `src/kazusa_ai_chatbot/rag/memory_retrieval_tools.py`
  - preserve the repository-owned typed memory metadata needed by the approved
    projection contract in both semantic and keyword search results;
  - keep embeddings out of the tool result and keep the exact repository row
    identifier available only for trace projection, not semantic authority or
    prompt prose.
- `src/kazusa_ai_chatbot/rag/memory_evidence/workers/user_memory.py`
  - emit the canonical explicit `source_kind="user_memory_units"` at the
    existing current-user evidence projection boundary;
  - add no downstream inference, compatibility fallback, storage mutation,
    model call, retry, or prompt content.
- `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag_projection.py`
  - partition learned memory by typed authority and preserve metadata.
- Direct subsystem READMEs under the owners above.
- `tests/ownership/source_test_impact_manifest.json`
  - map every changed production owner to the exact deterministic nodes below.
- Existing and planned tests named in `Test Impact And Traceability`.
- `tests/test_reflection_cycle_stage1c_reflection_context.py`
  - prove the bounded context carrier preserves the canonical certificate and
    excludes source-content records.
- `development_plans/README.md` and this plan for lifecycle/evidence.

### Create

- `src/scripts/audit_character_memory_scope.py`
  - bounded read-only export and offline semantic scope audit.
- `src/scripts/repair_character_memory_scope.py`
  - exact approved-manifest backup, drift validation, lifecycle rejection,
    cache verification, and re-audit.
- `tests/test_consolidation_character_self_guidance_scope.py`
- `tests/test_consolidation_prompt_language_contract.py`
- `tests/test_character_memory_scope_migration.py`
- `tests/test_cognitive_episode_contract.py`
- `tests/test_msg_decontextualizer.py`
- `tests/test_decontextualizer_referents.py`
- `tests/test_decontextualizer_live_llm.py`
- Required durable live-LLM and review artifacts under `test_artifacts/`.

### Keep

- `src/kazusa_ai_chatbot/cognition_shared/emotion_definitions.py`
- `src/kazusa_ai_chatbot/cognition_shared/emotion_derivation.py`
- `src/kazusa_ai_chatbot/cognition_shared/state_models.py`
- `src/kazusa_ai_chatbot/cognition_shared/state_reducers.py`
- Conversation Progress storage schema, TTL, scope key, event ledger, cache,
  and compaction.
- Existing database collections, indexes, and memory unit enum.
- Character identity-growth ownership and ledger.
- Relationship reducers, adapters, delivery, scheduler, resolver, relevance,
  and control console.
- Existing public database export and `reject_memory_unit(...)` operations.

### Delete

- No production module, persistent field, collection, database row, or test
  artifact.
- Old active plan entries leave the active registry and remain immutable in
  `archive/superseded/`.

## Test Impact And Traceability

| Source or governed path | Contract and semantic owner | Exact deterministic pytest nodes | Supplemental live nodes | Regression prevented |
|---|---|---|---|---|
| `src/kazusa_ai_chatbot/conversation_progress/recorder.py` | Scene/event observation and proposal ownership | `tests/test_conversation_progress_recorder.py::test_scene_prompt_defines_paraphrased_visible_move_repetition_without_future_planning`; `tests/test_conversation_progress_recorder.py::test_recorder_prompts_keep_unanswered_character_proposal_out_of_user_owned_decision_state` | `tests/test_semantic_response_progression_live_llm.py::test_live_recorder_recognizes_semantic_paraphrase_without_planning`; `tests/test_semantic_response_progression_live_llm.py::test_live_unanswered_character_proposal_stays_non_decision_continuity`; `tests/test_semantic_response_progression_live_llm.py::test_live_explicit_user_response_can_reopen_character_proposal` | Repetition is missed, or Asuna's proposal becomes the user's goal/decision constraint. |
| `src/kazusa_ai_chatbot/conversation_progress/projection.py` | Exact bounded move projection | `tests/test_conversation_progress_cognition_evidence.py::test_overused_move_projection_preserves_first_four_model_authored_rows_exactly` | Service Gate S1 | Reordered, rewritten, or unbounded move evidence reaches cognition. |
| `src/kazusa_ai_chatbot/conversation_progress/policy.py` | Four-row and 120-character limits | `tests/test_conversation_progress_cognition_evidence.py::test_overused_move_projection_preserves_first_four_model_authored_rows_exactly` | Gate B1 | New context displaces existing affect or continuation state. |
| `src/kazusa_ai_chatbot/conversation_progress/__init__.py` | Public projection export | `tests/test_conversation_progress_cognition_evidence.py::test_overused_move_projection_public_contract_is_exact_and_bounded` | none | Callers use divergent private projection paths. |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_cognition.py` | Cognition input and typed learned-memory authority across both memory ingress paths | `tests/test_conversation_progress_cognition_evidence.py::test_cognition_input_receives_exact_current_participant_overused_moves`; `tests/unit/nodes/test_persona_supervisor2_cognition_commit.py::test_rag_memory_authority_maps_self_guidance_to_conditional_context`; `tests/unit/nodes/test_persona_supervisor2_cognition_commit.py::test_promoted_reflection_context_rejects_incomplete_legacy_certificate`; `tests/unit/nodes/test_persona_supervisor2_cognition_commit.py::test_promoted_reflection_context_maps_certified_rows_through_typed_memory_contract` | Service Gates S1 and X1 | Wrong participant/move packet, an uncertified duplicate ingress, or global self-guidance becomes direct permission-bearing fact. |
| `src/kazusa_ai_chatbot/cognition_episode.py` | Exact ephemeral response-operation carrier and selected-operation equality | `tests/test_cognitive_episode_contract.py::test_dialog_response_operation_uses_response_content_provider_role` | Fresh decontextualizer provider gate; Service Gates S1H/S1I | Ambiguous ownership vocabulary becomes user rights transfer, or the cutover creates an alias, role reversal, or persisted-data dependency. |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_msg_decontextualizer.py` | One-call role-explicit meaning and response-content provider projection | `tests/test_msg_decontextualizer.py::test_decontextualizer_prompt_names_response_content_provider_role`; existing exact decontextualizer role/referent nodes updated big-bang | `tests/test_decontextualizer_live_llm.py::test_live_decontextualizer_emits_response_content_provider_contract` | The model emits the retired key, loses required selection, or reverses response/embedded roles. |
| `src/kazusa_ai_chatbot/cognition_core_v3/facade.py` | Canonical move input, shared A1/A2/G/P current-observation and request-agency authority, shared G/P background authority, and unchanged state transaction | `tests/unit/cognition_core_v3/test_handleless_contract.py::test_canonical_input_requires_bounded_overused_moves_without_exposing_handles`; `tests/unit/cognition_core_v3/test_handleless_contract.py::test_a1_system_prompt_and_packet_share_current_authority_contract`; `tests/unit/cognition_core_v3/test_handleless_contract.py::test_goal_and_plan_system_prompts_share_background_goal_authority_contract`; `tests/unit/cognition_core_v3/test_handleless_contract.py::test_all_stage_system_prompts_share_request_agency_authority_contract` | `tests/test_semantic_response_progression_live_llm.py::test_live_stable_memory_does_not_become_current_event_or_response_goal`; `tests/test_semantic_response_progression_live_llm.py::test_live_advice_request_preserves_user_agency_without_dependency_inference`; `tests/test_semantic_response_progression_live_llm.py::test_live_explicit_scoped_delegation_preserves_characterful_agency_response`; `tests/test_semantic_response_progression_live_llm.py::test_live_multi_emotion_context_preserves_original_design` | A higher-priority instruction restores stable-memory current-event or final-goal authority; an ordinary contribution request becomes agency surrender or relationship dependence; a real scoped delegation is suppressed; progression evidence leaks; call count or emotion state changes. |
| `src/kazusa_ai_chatbot/cognition_core_v3/prompt.py` | A1/A2/G/P current-delta, request-agency, response-content provider, character-subject existential appraisal, field-local G provenance, background-goal, stable-memory, correction, and recipient authority | `tests/unit/cognition_core_v3/test_prompt_context.py::test_overused_moves_reach_participant_continuity_after_a1_only`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_goal_guidance_progresses_current_delta_and_preserves_deliberate_reopening`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_a1_guidance_separates_current_observation_from_stable_direct_facts`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_goal_and_plan_packets_share_background_goal_authority_contract`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_request_agency_authority_contract_reaches_all_cognition_stage_packets`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_response_content_provider_is_reply_content_fact_across_all_stages`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_a2_existential_drive_keeps_character_experience_distinct_from_user_state`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_g_relational_carriers_separate_character_motive_from_user_relationship_fact`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_user_owned_semantic_correction_guides_a1_goal_and_plan_without_hidden_intent_assertion`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_recipient_scoped_permission_rule_reaches_a2_goal_and_plan`; `tests/unit/cognition_core_v3/test_prompt_context.py::test_semantic_progression_context_preserves_all_existing_multi_affect_rows_and_causes` | Service Gates S1H/S1I, S2, X1, and B1; one fresh ordinary response-content negative and one explicit scoped-delegation positive under the frozen provider contract | Stable memory is promoted into a current event, added objective, or final stance; response content becomes external user-agency transfer; A2 writes user state into character-owned appraisal; G invents user trust/dependence through first-person carriers; explicit scoped delegation loses its intended character response; correction is inverted; participant A's permission transfers to B; affect is flattened. |
| `src/kazusa_ai_chatbot/cognition_shared/transition_guards.py` | Knowledge-gap FSM / actual accepted uncertainty decrease and same-episode recurrence safety | `tests/unit/cognition_core_v3/test_state_transaction.py::test_zero_clamped_gap_decrease_does_not_terminalize_before_same_episode_recurrence` | Fresh Service Gate S1G only; preserve failed S1F without rerun | A zero-to-zero clamped decrease falsely resolves a new gap, and a later resolver pass recreates its canonical ID and aborts the service turn. |
| `src/kazusa_ai_chatbot/cognition_shared/contracts.py` | Canonical cognition-to-surface input | `tests/unit/nodes/test_persona_supervisor2_l3_surface.py::test_text_surface_input_requires_exact_bounded_overused_moves` | Gate L1 | Alias/fallback surface shapes or malformed move lists appear. |
| `src/kazusa_ai_chatbot/cognition_shared/surface.py` | Exact deterministic L3 payload | `tests/unit/nodes/test_persona_supervisor2_l3_surface.py::test_surface_payload_projects_exact_overused_moves` | Gate L1 | L3 loses or rewrites accepted progression evidence. |
| `src/kazusa_ai_chatbot/cognition_shared/surface_stages.py` | One-call content planning, selected-goal fidelity, and interpretation-reason visibility boundary | `tests/unit/nodes/test_persona_supervisor2_l3_surface.py::test_text_surface_progression_contract_keeps_one_semantic_call`; `tests/unit/nodes/test_persona_supervisor2_l3_surface.py::test_content_plan_keeps_goal_reason_outside_visible_semantic_authority` | `tests/test_semantic_response_progression_live_llm.py::test_live_l3_does_not_reintroduce_unselected_semantic_payoff`; `tests/test_semantic_response_progression_live_llm.py::test_live_advice_request_preserves_user_agency_without_dependency_inference` | L3 adds an evaluator/retry, treats goal reasons as independent visible meaning, or restores an unselected payoff. |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_l3_surface.py` | Current-participant move handoff | `tests/unit/nodes/test_persona_supervisor2_l3_surface.py::test_l3_surface_receives_exact_current_participant_overused_moves` | Gate L1 | L3 receives stale or cross-participant move evidence. |
| `src/kazusa_ai_chatbot/nodes/dialog_agent.py` | Final wording within selected semantics | `tests/unit/nodes/test_dialog_agent.py::test_dialog_creative_expansion_cannot_add_unselected_stance_or_relationship_payoff` | Gate L1 visible output | Dialog invents a relationship, intention, or payoff absent from the plan. |
| `src/kazusa_ai_chatbot/consolidation/memory_units.py` | Router-approved candidate-type contract and character-owned commitment semantics | `tests/test_user_memory_units_rag_flow.py::test_extractor_contract_limits_candidate_types_to_router_enabled_lanes`; `tests/test_user_memory_units_rag_flow.py::test_valid_candidates_rejects_active_commitment_outside_enabled_lane`; `tests/test_user_memory_units_rag_flow.py::test_extractor_prompt_requires_character_owned_acceptance_and_rejects_silence` | `tests/test_user_memory_units_live_llm.py::test_live_extractor_does_not_invent_commitment_from_unanswered_character_proposal`; `tests/test_user_memory_units_live_llm.py::test_live_extractor_preserves_explicit_character_owned_commitment` | A user-memory route writes a false commitment or legitimate accepted commitment is suppressed. |
| `src/kazusa_ai_chatbot/consolidation/lane_router.py` | Coarse global/user/group/transient scope routing | `tests/test_consolidation_lane_router_contract.py::test_lane_router_distinguishes_global_user_group_and_transient_behavior_rules` | `tests/test_consolidation_memory_write_use_cases_live_llm.py::test_live_case_07_user_specific_address_rule_routes_commitment`; `tests/test_consolidation_memory_write_use_cases_live_llm.py::test_live_case_11_accepted_repetition_rule_routes_character_self_guidance`; `tests/test_consolidation_memory_write_use_cases_live_llm.py::test_live_case_12_unaccepted_repetition_rule_writes_nothing`; `tests/test_consolidation_memory_write_use_cases_live_llm.py::test_live_case_15_group_specific_norm_routes_group_style`; `tests/test_consolidation_memory_write_use_cases_live_llm.py::test_live_case_16_one_turn_roleplay_instruction_writes_nothing` | Scoped or temporary behavior routes to global self-guidance. |
| `src/kazusa_ai_chatbot/consolidation/character_self_guidance.py` | Independent global-scope specialist/reviewer admission | `tests/test_consolidation_character_self_guidance_scope.py::test_self_guidance_write_requires_independent_global_scope_agreement`; `tests/test_consolidation_character_self_guidance_scope.py::test_self_guidance_persists_reviewer_scope_and_privacy_review` | `tests/test_consolidation_memory_write_use_cases_live_llm.py::test_live_case_13_global_character_response_rule_routes_self_guidance`; Gate X1 | One optimistic model decision globalizes a target-specific relationship rule. |
| `src/kazusa_ai_chatbot/memory_evolution/models.py` | Learned-memory privacy/scope certificate | `tests/test_consolidation_character_self_guidance_scope.py::test_learned_memory_privacy_review_requires_exact_scope_certificate` | none | Learned rows claim global/privacy safety without typed evidence. |
| `src/kazusa_ai_chatbot/reflection_cycle/promotion.py` | Independent target-removal/privacy review for promotion | `tests/test_reflection_cycle_stage1c_promotion.py::test_reflection_memory_write_requires_independent_global_scope_review`; `tests/test_reflection_cycle_stage1c_promotion.py::test_identity_or_boundary_candidate_never_writes_self_guidance`; `tests/test_reflection_cycle_stage1c_promotion.py::test_promotion_evidence_carries_source_privacy_notes_without_assuming_low_risk` | `tests/test_reflection_cycle_stage1c_promotion_live_llm.py::test_global_promotion_live_rejects_target_specific_behavior_after_target_removal`; `tests/test_reflection_cycle_stage1c_promotion_live_llm.py::test_global_promotion_live_normal_case` | Relationship-derived or privacy-unknown behavior becomes global memory. |
| `src/kazusa_ai_chatbot/reflection_cycle/context.py` | Lossless bounded carrier from active promoted repository rows to cognition admission | `tests/test_reflection_cycle_stage1c_reflection_context.py::test_reflection_context_preserves_typed_authority_certificate` | Service Gates S1 and X1 protected input | The live-chat context loader strips the W3 certificate before cognition and creates a second uncertified authority path. |
| Consolidation and reflection-promotion runtime prompt constants | Native Simplified-Chinese prose with byte-exact contractual tokens | `tests/test_consolidation_prompt_language_contract.py::test_consolidation_runtime_prompts_use_chinese_prose_and_preserve_contract_tokens`; `tests/test_consolidation_prompt_language_contract.py::test_consolidation_runtime_prompts_contain_no_captured_case_language` | One-at-a-time lane-router, extractor, merge/rewrite, stability, self-guidance, and promotion live gates | Mixed-language stages cause implicit translation, terminology drift, or lost ownership/scope constraints. |
| `src/kazusa_ai_chatbot/rag/memory_retrieval_tools.py` | Lossless typed-memory retrieval boundary before prompt-safe projection | `tests/test_memory_retrieval_tools.py::test_persistent_memory_tools_preserve_typed_authority_and_scope_metadata` | Gate X1 protected retrieval | The database returns typed scope/authority evidence but the tool rebuild drops it, making later authority classification fixture-only. |
| `src/kazusa_ai_chatbot/rag/memory_evidence/workers/user_memory.py` | Explicit canonical source identity for current-user evidence | `tests/test_user_memory_evidence_agent.py::test_user_memory_project_row_preserves_contextual_fields` | Gate X1 protected retrieval | The canonical worker emits every typed scope/authority field except `source_kind`, so strict downstream admission drops truthful current-user continuity or is tempted to infer missing metadata. |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag_projection.py` | Typed learned-memory partition and metadata preservation | `tests/test_rag_projection.py::test_shared_memory_projection_partitions_fact_and_self_guidance_with_typed_metadata`; `tests/test_shared_memory_prewarm.py::test_first_cycle_prewarm_preserves_self_guidance_conditional_authority` | Gate X1 protected input | Fact and self-guidance collapse into one direct-fact row or prewarm bypasses authority. |
| `src/scripts/audit_character_memory_scope.py` | Read-only exact scope audit | `tests/test_character_memory_scope_migration.py::test_audit_is_read_only_and_emits_exact_scope_manifest`; `tests/test_script_db_boundary.py::test_scripts_do_not_import_raw_or_private_db_boundary` | Read-only production audit | Diagnosis mutates data, loses lineage, or bypasses public DB boundaries. |
| `src/scripts/repair_character_memory_scope.py` | Drift-safe exact lifecycle apply | `tests/test_character_memory_scope_migration.py::test_apply_rejects_only_approved_unchanged_units_and_invalidates_cache`; `tests/test_character_memory_scope_migration.py::test_apply_stops_on_manifest_row_drift`; `tests/test_script_db_boundary.py::test_scripts_do_not_construct_raw_mongo_query_or_update_dsl` | Categorically authorized Asuna test-DB smoke after backup | Repair touches rows outside the certified legacy predicate, mutates a drifted row, or leaves rejected memory cached. |
| `tests/ownership/source_test_impact_manifest.json` | Exact changed-source governance | `tests/test_test_impact_manifest.py::test_manifest_covers_strict_cognition_source_boundary`; `tests/test_test_impact_manifest.py::test_stale_required_node_fails_closed` | none | A production owner lacks a collectable exact deterministic test. |

Every planned new node is created before its production owner changes. The
executor records genuine RED where the current contract fails, then GREEN.
All changed production paths must collect and run their exact mapped nodes
before acceptance.

## Required Real-LLM And Service Gates

Each case runs separately, emits a durable artifact containing input, model
configuration, rendered prompt or version, raw output, parsed output, relevant
state, and judgment notes, and receives parent inspection before the next case.

### R1 — recorder unanswered-proposal negative control

Use a character-authored bargain and a user turn that continues the underlying
task without responding to it. The real scene/event recorders must keep the
proposal outside user goal/blocker and outside `decision` relevance.

### R2 — recorder explicit-response positive control

The user explicitly accepts, rejects, negotiates, or asks about the proposal.
The recorder may treat it as current decision evidence while preserving exact
actor, precondition, and lifecycle ownership.

### M1 — captured false-commitment negative control

Run the real extractor on the captured task/reward context with only
`user_memory_units` enabled. It must emit no active commitment and no claim that
the user accepted or owes a reward. A legitimate workload memory or an empty
list is acceptable.

### M2 — accepted character-commitment positive control

Use a user-scoped request and visible character acceptance with only
`active_commitment` enabled. The extractor must preserve one character-owned
commitment and its source/time contract.

### S1 — private memory-enabled semantic release

Run the four-turn L2 sequence twice under independent fresh identities. One
initial personality tactic may appear naturally; changed connected turns must
advance their current questions without making that tactic the consecutive
dominant payoff. Post-turn memory must contain no fabricated agreement.

### S2 — correction and deliberate-continuation controls

Run the explicit current-user correction gate twice and the deliberate
continuation positive control once. Correction cannot become proof of its
opposite. Explicit user reopening must still allow a grounded prior theme to
continue and deepen.

### L1 — L3/dialog non-reintroduction

Use an accepted G/P goal that excludes a prior relational payoff. Content plan
and dialog must preserve character voice without restoring that payoff or
adding an unselected user intention, relationship, or commitment.

### X1 — cross-user recipient applicability

Use a group-shaped debug scenario with two distinct stable users. Participant
A supplies a relationship, consent, or permission statement addressed to A.
Participant B later requests analogous access. Protected packets must preserve
both identities and addressees; Asuna must judge B from B's current evidence
and cannot cite A's agreement as B's permission. A separate positive control
allows genuinely target-free global self-guidance to affect tone or tactics.

### P1 — promotion target-removal controls

Run one target-specific relationship-derived candidate and one genuinely
target-free character-guidance candidate through the real promoter/reviewer
pair. The first receives no learned global write candidate; the second may pass
only with matching scope/privacy certificates.

### B1 — bounded path and multi-emotion preservation

Run real `run_cognition` with at least five simultaneous event-root emotions:
sadness, anger, gratitude, embarrassment, and nostalgia, each with a distinct
active cause, plus four maximum-size move descriptors. A1/A2/G/P continuation
and affect rows must remain exactly equal in identity, order, phase, intensity,
trend, cause status, and cause summary. The latest observation must still own
the selected response goal. The call roster, prompt caps, and no-retry contract
must remain exact.

### D1 — read-only audit and exact apply gate

Run the audit against current learned global memory and prove zero writes. The
parent reviews every disposition and presents the exact apply manifest. Apply
execution begins only after user approval of that manifest. The post-apply
audit must show approved-invalid rows inactive, valid global controls active,
and cache state refreshed.

## Execution Work Items

### W0 — consolidated baseline and inherited implementation review

- Record worktree status, exact owned paths, current hashes, service state, and
  attributable diffs from the superseded semantic-progression plan.
- Re-run the exact deterministic nodes already reported green.
- Preserve the existing failed L2 evidence as immutable RED.
- Exit: parent confirms the inherited baseline matches this plan.

### W1 — Conversation Progress authority

- Add exact tests and record RED.
- Implement the scene/event prompt contract in `recorder.py` only.
- Run deterministic owner nodes, R1, and R2.
- Exit: both live recorder controls pass with unchanged schemas and call count.

### W2 — commitment lane authority

- Add exact tests and record RED.
- Implement the dynamic allowed-type contract and commitment semantics in
  `memory_units.py` only.
- Run deterministic owner nodes, M1, and M2.
- Exit: the captured false commitment is impossible under `user_memory_units`
  while legitimate character-owned commitments remain available.

### W3 — learned-memory scope and typed retrieval

- Implement write-time scope certificates, reflection review, typed projection,
  recipient guidance, and exact owner tests within the declared surface.
- Preserve that certificate through both the search-driven RAG path and the
  bounded promoted-reflection context path, and admit both through the same
  canonical typed-memory classifier.
- Keep stable character/world memory as background truth or conditional
  guidance; only current observation may establish what the current user is
  doing, accepting, permitting, or asking the character to pursue now.
- Run the existing lane-routing controls, P1, and direct projection tests.
- Exit: target-specific learned meaning fails closed; valid global guidance
  reaches cognition conditionally; every uncertified legacy row is absent from
  cognition; foreground call count stays unchanged.

### W3L — consolidation prompt language unification

- Localize all runtime prompt prose in `consolidation/` and W3 reflection
  promotion while preserving contractual tokens and every semantic boundary.
- Preserve the canonical background-source boundary from the completed Stage 3
  adoption: `scheduled_tick` is a claimed calendar commitment, while reflection
  remains an offline producer with direct style and promotion owners. Do not
  broaden the consolidation router roster to recreate the retired
  `reflection_signal` trigger path.
- Correct the mechanically migrated reflection-style router fixture into a
  no-leak negative control: reflection-derived user style presented on a
  `scheduled_tick` must not become user memory or any other consolidation lane.
  The direct reflection-cycle interaction-style owner remains unchanged.
- Run the shared deterministic language-contract tests and each distinct LLM
  stage's real control one at a time; inspect raw outputs for translation loss.
- Exit: no non-contractual English prompt prose remains, no case language has
  entered a prompt, and schema, semantics, call count, and output quality match
  the accepted pre-localization behavior.

### W4 — integrated service and multi-emotion gates

- Reload the candidate service after W1-W3 deterministic/live component gates.
- Before another service repetition, preserve S1F as an immutable operational
  RED and correct its independently reproduced knowledge-gap recurrence
  invariant test-first. The first pass and the final resolver pass retain the
  same current-episode root identity; a zero-clamped negative uncertainty
  request cannot count as an actual decrease or terminal transition. Preserve
  recurrence over the prior replacement state, canonical IDs, strict terminal
  guards, state schemas, relationship maintenance, affect derivation, and the
  final-only commit boundary.
- Preserve S1G as operational GREEN and semantic RED. Its current-turn carrier
  is ephemeral, so replace the ambiguous provider key big-bang across the
  decontextualizer, episode validator, cognition contract, tests, and ICD. Add
  no compatibility alias. At A2, make every `existential_drive` meaning and
  axis explicitly about the current character's experience. At G, bind
  `relational_willingness.reason` to character-side motive,
  `cause_summary` to current facts, and `private_monologue` to the character's
  own experience without inventing user trust, dependence, need, ability, or
  surrendered agency.
- Run S1, S2, L1, X1, and B1 one at a time under fresh identities.
- Inspect protected foreground and background traces and post-turn memory.
- If an integrated trace keeps the current practical goal in G/P but restores
  background relationship meaning as explicit visible content, apply one
  shared native-Chinese visible-authority contract to L3 content planning and
  dialog rendering. `relational_willingness` and `private_monologue` continue
  to shape warmth, initiative, directness, distance, and voice; explicit
  relationship meaning or payoff is visible only when the current observation
  and selected response goal make that meaning part of the current reply.
- If an integrated trace treats an ordinary request for advice, information,
  comparison, reminders, or options as proof that the user surrendered agency,
  lacks independent judgment, became dependent, increased trust, or deepened a
  relationship, apply one shared native-Chinese request-agency authority
  contract across A1, A2, G, and P. The default meaning of such a request is a
  bounded current contribution while the user retains choice and action
  ownership. An explicit one-time delegation establishes only that stated
  scope; wider dependence, ability, trust, relationship, or continuing
  authority requires its own current evidence.
- Keep `active_character_goal.reason`, `active_character_goal.cause_summary`,
  `intention.reason`, `relational_willingness`, and `private_monologue` as
  interpretation or delivery context at L3. They cannot independently expand
  the visible semantics selected by the current observation and
  `response_plan.response_goal`.
- The L3/dialog correction stays inside the existing one-call content planner
  and one-call dialog renderer. It adds no evaluator, retry, deterministic
  semantic filter, post-processor, model stage, or compatibility path. Any
  English explanatory prose in the touched surface-prompt block is localized
  to native Simplified Chinese while schema and contractual tokens stay exact.
- Create the exact deterministic prompt-contract nodes before production edits,
  record RED/GREEN, freeze the generalized prompt before authoring a distinct
  focused live negative control and an explicit scoped-delegation positive
  control, inspect each once, then run both post-provider-cutover S1H/S1I
  repetitions under independent fresh identities. Preserve every earlier live
  artifact and do not use a stochastic rerun to replace a RED.
- Exit: every semantic, identity, commitment, call-budget, and affect gate
  passes in all required repetitions.

### W5 — read-only data audit

- Implement audit/repair tools and deterministic tests.
- Run only the read-only audit and prepare raw plus readable artifacts.
- Exit: parent verifies the exact manifest is wholly covered by the user's
  categorical legacy-row removal authority.

### W6 — exact data apply and final sign-off

- Back up, drift-check, lifecycle-reject every unchanged manifest row covered
  by the categorical test-DB authority, invalidate cache, and re-audit.
- Run the final cross-user positive/negative smoke and independent code review.
- Exit: all acceptance evidence is complete and the user accepts lifecycle
  closure.

## Execution Roles

### `architecture_acceptance_owner`

- Responsibility: system ownership, scope decisions, plan lifecycle, test
  message selection, raw evidence review, and final user-facing recommendation.
- Owned surface: this plan, lifecycle registry, RCA/review artifacts, and
  read-only inspection of every declared source/test/data boundary.
- Authority: plan/document edits, read-only diagnostics, test-quality verdicts,
  handoff decisions, and acceptance/rejection of executor output. Production or
  database changes require the user authorities stated in this plan.
- Applicable skills: development-plan, local-llm-architecture,
  no-prepost-user-input, character-test, llm-trace-debug,
  database-data-pull, and test-style-and-execution.
- Capability floor: senior system architecture, LLM prompt/authority design,
  MongoDB evidence interpretation, protected-trace review, and multi-stage
  test judgment.
- Independence requirement: separate from the production implementation role.
- Acceptance output: current plan checkpoints, reviewed diffs/traces, quality
  verdicts, exact data manifest, and final sign-off recommendation.
- Gate: user approves this plan before production handoff; every work item exits
  only after its exact evidence passes parent review.

### `production_and_test_executor`

- Responsibility: test-first implementation inside the declared change
  surface, deterministic verification, one-at-a-time real-LLM execution, and
  factual handoff evidence.
- Owned surface: every production/test/document path in `Change Surface`,
  divided into W1-W5 handoffs with explicit file ownership at runtime.
- Authority: modify approved files, run tests, start/reload the local candidate
  service, send parent-approved or parent-selected debug test messages, and
  produce artifacts. Database mutation authority remains absent until W6.
- Applicable skills: py-style, cjk-safety, local-llm-architecture,
  no-prepost-user-input, test-style-and-execution, character-test,
  llm-trace-debug, database-data-pull, and development-plan execution gates.
- Capability floor: GPT-5.6 Luna, maximum reasoning, standard normal speed,
  full repository/tool access, production-quality Python, and real-LLM trace
  inspection. This is the user-fixed execution constraint.
- Independence requirement: cannot provide independent final code review.
- Acceptance output: RED/GREEN evidence, scoped diff, collected exact nodes,
  one-at-a-time live artifacts, call/prompt budgets, and handoff summary.
- Gate: parent supplies a bounded work item and baseline; any unplanned
  production path or semantic contract pauses for parent analysis and explicit
  user approval.

### `independent_code_reviewer`

- Responsibility: read-only review of the completed W1-W5 implementation and
  evidence against this plan.
- Owned surface: read-only access to the complete attributable diff, mapped
  tests, raw live artifacts, audit artifacts, and plan contracts.
- Authority: report severity-ranked findings and pass/fail the review gate;
  no remediation edits.
- Applicable skills: development-plan review, local-llm-architecture,
  no-prepost-user-input, py-style, and test-style-and-execution.
- Capability floor: independent senior code/LLM architecture review with
  access to the full diff and evidence.
- Independence requirement: a runtime agent distinct from the implementation
  executor.
- Acceptance output: written findings and approve/revise verdict.
- Gate: starts after W5 evidence; every required finding is remediated by the
  implementation role and re-reviewed before W6 closure.

### `exact_data_remediation_operator`

- Responsibility: execute only the categorically authorized exact manifest through the
  tested repair tool and capture backup/post-apply evidence.
- Owned surface: approved manifest rows, backup artifact, repair tool command,
  cache verification, and post-apply audit.
- Authority: reject only exact unchanged memory-unit IDs matching the approved
  legacy learned-memory predicate after W6 entry. No rewrite, re-home, physical
  deletion, or unrelated mutation.
- Applicable skills: database-data-pull, development-plan execution gates, and
  test-style-and-execution.
- Capability floor: exact-manifest validation, public memory lifecycle API,
  backup/recovery evidence, and live database verification.
- Independence requirement: parent independently verifies the manifest and
  post-apply result.
- Acceptance output: backup, per-row disposition, cache result, post-audit, and
  drift/no-drift report.
- Gate: parent proves every manifest row matches the categorical authority;
  any row drift stops the entire apply before additional mutation.

## Agent Autonomy Boundaries

- The Luna executor may choose local helper names, prompt sentence order,
  internal test-fixture decomposition, command order, and artifact filenames
  while preserving every contract, path boundary, call count, and exact test
  node.
- The Luna executor may not add a production path, field concept, stage, call,
  retry, evaluator, compatibility alias, fallback, database operation,
  relationship reducer change, adapter change, or deterministic natural-
  language classifier.
- The parent may choose ordinary live-test messages and adapt the next message
  from inspected evidence. A semantic-quality decision requiring the user's
  subjective judgment is presented to the user.
- A required live failure remains RED. A different stochastic pass does not
  erase it. The parent classifies the failure and either keeps remediation
  inside the fixed contract or requests a plan amendment.
- The database operator applies only an exact manifest whose every row matches
  the user's categorical test-DB legacy predicate. Scope discovery, row
  classification, and audit remain read-only.

## Verification

1. Collect every exact deterministic node in the traceability matrix.
2. Run the smallest exact nodes after each test-first RED and production GREEN.
3. Run mapped adjacent deterministic tests for each completed work item.
4. Validate UTF-8 AST and runtime rendering for every changed CJK prompt.
5. Run the consolidation prompt-language contract: native Simplified-Chinese
   prose, byte-exact contractual tokens, no bilingual duplication, and no
   captured-case vocabulary.
6. Run `tests/test_test_impact_manifest.py` and
   `venv\Scripts\python.exe -m scripts.validate_test_impact --check-all --run`.
7. Run `git diff --check` and changed-path scope review.
8. Run every real-LLM and service gate separately with artifact inspection;
   every localized consolidation stage receives at least one direct real-LLM
   control, and the lane-router use-case matrix runs one case at a time.
9. Compare foreground/background call rosters, prompt sizes, and database-read
   counts to the baseline.
10. Run the available non-live suite after all exact and adjacent owner gates.
11. Perform independent read-only code review and resolve every required
    finding.
12. Run read-only audit, categorical-authority manifest gate, approved apply, and
  post-apply audit in that order.

## Acceptance Criteria

1. Asuna remains recognizably characterful and memory-influenced while changed
   user turns own their current response contribution.
2. Unanswered character-authored proposals remain factual scene/history
   continuity and never become user goals, blockers, decision constraints, or
   commitments.
3. Explicit user response or deliberate reopening still permits grounded
   continuation of a prior theme.
4. `user_memory_units` alone cannot persist an `active_commitment` candidate.
5. Legitimate character-owned, user-scoped accepted commitments remain
   available and source-grounded.
6. Distinct group users remain distinct in identity, addressee, participant
   continuity, relationship, consent, permission, promise, and role semantics.
7. Valid global facts remain direct fact evidence; valid self-guidance remains
   conditional; incomplete or mixed learned metadata fails closed.
8. Target-specific, private, relationship, identity, or boundary meaning cannot
   become global character memory through redaction alone.
9. The exchange-condition promoted row and every audited row receive an exact
   review disposition; database changes affect only later user-approved IDs.
10. A1/A2/G/P, L3, dialog, recorder, extractor, and reflection calls remain
    within the declared overhead budget.
11. The original five-emotion real-LLM gate preserves exact continuation and
    affect rows, cause order, and mixed emotional motivation.
12. Every changed production path has a collected and passing exact
    deterministic owner node plus required supplemental live evidence.
13. No deterministic prose classifier, semantic postfilter, evaluator, retry,
    compatibility layer, whole-database erase, or hidden fallback is present.
14. Every changed runtime prompt passes a generalization audit: each added
    sentence states a domain-general ownership invariant, contains no captured
    scenario or test-derived wording, and has tests whose assertions do not
    mirror prompt phrases.
15. Every consolidation and W3 reflection-promotion runtime prompt uses native
    Simplified-Chinese prose while preserving all contractual tokens, schemas,
    semantic ownership, negation, scope, stage count, and call budget; direct
    real-LLM controls show no translation loss.
16. Independent review has no unresolved required finding.
17. The user accepts the final evidence and lifecycle closure.

## Rollback And Recovery

- Each work item records pre-edit hashes and its explicitly owned paths.
- Code rollback uses a reviewed patch against only the attributable work-item
  diff and preserves all user/concurrent changes and evidence artifacts.
- Live-gate failures preserve their identities, traces, outputs, and post-turn
  state for diagnosis.
- Database apply creates a backup before the first lifecycle change and stops
  on any hash/status drift. Rejected rows remain recoverable lifecycle records;
  reactivation requires new explicit user authority.
- A failed or withdrawn consolidated plan remains in active draft/in-progress
  state until the user directs supersession; it is never represented as
  completed.

## Progress Checklist

- [x] Read current project architecture, relevant source/tests, active plans,
  planning registry, dirty worktree, and historical design lineage.
- [x] Collect latest-anchored 24-hour QQ evidence and confirm distinct user
  identity binding versus semantic applicability failure.
- [x] Reproduce semantic fixation with a fresh memory-enabled service identity.
- [x] Implement the first bounded progression cutover and deterministic tests
  under the superseded plan.
- [x] Preserve multi-emotion structure in deterministic coverage.
- [x] Run the exact post-amendment L2 replay and record the remaining RED.
- [x] Identify Conversation Progress decision-authority conflict, false
  active-commitment persistence, and promoted-memory feedback pressure.
- [x] Consolidate current scope and archive obsolete active planning records.
- [x] User reviews, approves, and commands execution of this consolidated
  plan.
- [x] W0 inherited baseline review passes.
- [x] W1 Conversation Progress authority passes R1/R2 and the prompt
  anti-cheating audit.
- [x] W2 commitment lane authority passes M1/M2 and the prompt
  anti-cheating audit.
- [x] W3L consolidation prompt language unification
  passes deterministic and one-at-a-time real-LLM gates.
- [x] W3 search-driven RAG typed retrieval passes component gates.
- [x] W3 promoted-reflection context ingress uses the same certified contract,
  and A1 keeps stable memory outside current-event authority.
- [x] W4 integrated gates received their permitted final execution. S1J and
  S2A/S2B remained semantic RED, S2C was semantic GREEN, and both X1 gates
  ended operational RED; prior accepted L1/B1 evidence was retained without
  rerun.
- [x] W5 read-only audit and exact proposed manifest complete.
- [x] User categorically authorizes lifecycle rejection of every unchanged
  Asuna test-DB legacy learned-memory row that fails the W3 certificate.
- [x] Approved W6 data apply and post-audit complete: all 349 authorized rows
  are lifecycle-rejected and both certified controls remain active. Cache-stat
  proof remained RED, and the terminal execution cap precluded another
  independent re-review.
- [x] The user required closure after the single final attempt regardless of
  outcome. The plan is archived as an executed record with residual failures;
  this closure is not functional acceptance.

## Final Closure Record

### Closure decision

- Closed: 2026-08-25 (Pacific/Auckland).
- Lifecycle completion: 100%. Functional acceptance: RED.
- Authority: the user allowed the remaining tests once, one consolidated
  remediation for all surfaced issues, one final verification attempt, and
  required plan closure regardless of that final result.
- The implementation and evidence were frozen after the final attempt. No
  failed node or live case was rerun, replaced, or repaired afterward.
- Closure records completed execution rather than successful behavioral
  sign-off. Any remediation of the residual ledger requires a new, separately
  approved plan.

### Frozen remediation

- The model-facing current-event projection exposes only
  `role_explicit_content`. Typed `response_operation` and
  `response_content_provider_role` remain deterministic protocol and
  validation data outside A1/A2/G/P semantic evidence.
- The provider field was removed from model-facing orientation and prompt
  prose. The hybrid A1/A2/G/P flow, L3/dialog topology, model-call count,
  personality, memory, relationship reducers, affect, and multi-emotion
  contracts remain structurally intact.
- W6 independently revalidates the W3 certificate and manifest/live fields
  before mutation. Both migration CLIs emit UTF-8 JSON on Windows.
- Reflection-promotion write-boundary logging includes the exception text.
- Runtime prompt changes contain no final fixture wording or scenario-shaped
  vocabulary.

### Final deterministic and static result

- Exact affected-node result: 16 passed, 2 failed.
- Preserved failures:
  - `test_response_content_provider_is_reply_content_fact_across_all_stages`
    retained a stale assumption that every packet contains `orientation`;
  - `test_a2_existential_drive_keeps_character_experience_distinct_from_user_state`
    retained a stale exact-token expectation for `current_observation`.
- UTF-8 compile/AST validation, manifest JSON validation, and
  `git diff --check` passed.
- Ruff remained RED with eight findings. Attributable findings include an
  unused `json` import after projection removal and the required exception
  text in `logger.exception`; the rest are preserved/pre-existing findings
  recorded in the handoff.

### W6 database result

- Input audit: 352 rows, 351 learned candidates, 349 W3-invalid active learned
  rows, 2 certified controls, and zero prior writes.
- Full-row hash, status, authority, source, type, and certificate preflight
  passed before the first mutation.
- The public lifecycle API rejected all 349 authorized rows. No row was
  physically deleted or rewritten.
- Post-audit: 352 rows, 2 learned candidates, 2 certified rows, and an empty
  repair manifest.
- Cache-stat evidence remained RED because the observed invalidation counter
  stayed `0 -> 0`.
- Backup SHA-256:
  `5631CEF80BE15B41E027E39835FFD366CC1F0782ACC99F95BC696D02DB7D3204`.
- Apply-report SHA-256:
  `0954CA885F691C7D01ECA6B3D6EA310AA763C3C8FBDC100AD7354E8D62A79670`.

### Final real-service result

| Gate | Operational result | Semantic result |
|---|---|---|
| S1J | GREEN; four delivered turns | RED: ordinary advice requests were still repeatedly promoted to transferred decision rights, responsibility, reliance, or role authority in A1/A2/G |
| S2A | GREEN; correction turn delivered | MIXED/RED: the correction kept decision and execution with the user, while the preceding ordinary request was still framed as transferred authority |
| S2B | GREEN; correction turn delivered | MIXED/RED: the correction produced bounded advice, while the preceding request for suggestions was still framed as partial authority transfer and reliance |
| S2C | GREEN | GREEN: explicit delegation remained confined to one title decision and its direct continuation |
| X1 negative | RED: HTTP 200 carried no messages, identity, or protected cognition trace after recipient-grounding failed closed | Not evaluable |
| X1 positive | RED: artifact capture failed on an unserialized Mongo `ObjectId` before a model call | Not evaluable; the synthetic memory row was lifecycle-rejected and cleanup was verified |

S1J wrote zero user memories and left semantic relationship axes unchanged.
The original failure therefore remains reproducible without user memory and
after legacy-row cleanup. Memory had amplified historical cases, but it is not
the sufficient root cause of the live semantic fixation.

### Residual root-owner ledger

1. Cognition semantic ownership still permits the local model to infer broad
   authority and being-needed meaning directly from ordinary bounded advice
   requests. Carrier separation was architecturally correct but insufficient.
2. Group recipient/relevance grounding stopped X1 negative before cognition,
   so distinct-user privacy behavior lacks final service evidence.
3. The X1 positive artifact harness does not normalize Mongo `ObjectId` values
   before JSON serialization.
4. Two deterministic prompt-context assertions remain stale against the new
   absence-based boundary.
5. Cache invalidation was invoked through the public lifecycle path but was
   not demonstrated by the process-local counter.
6. The provider continues to reject the first `json_object` format request and
   consume the existing bounded JSON-Schema fallback, adding latency.
7. Scoped Ruff acceptance remains RED.

### Immutable final evidence

- Factual handoff:
  `test_artifacts/execution/asuna_semantic_authority_final_pass_20250825/final_attempt_handoff.md`.
- Handoff SHA-256:
  `50F8E6459FF791A6A314D505F60DFC0A3D57DFED1DB0E90DFBDF25EA1F9DCA49`.
- W5 audit:
  `test_artifacts/execution/asuna_semantic_authority_final_pass_20250825/w5_readonly_audit/scope_audit.json`.
- W5 audit SHA-256:
  `B92F79E8D70CD96CB629B917904607739676EF138309540C6E962A4B75AD3F1C`.
- W6 backup, apply report, and post-audit are under
  `test_artifacts/execution/asuna_semantic_authority_final_pass_20250825/w6_apply/`.

## Execution Evidence Baseline

- Semantic-attractor RCA SHA-256:
  `1E23913D09C4744A4BFC0C15525E3BE8246F02238324460FE8352C7811395D84`.
- QQ memory/personality RCA SHA-256:
  `681A4C3113D0C0E5ED3F43E7D897D449931DCC70005D811D6E2F3A8112EB2285`.
- Post-remediation L2 RCA SHA-256:
  `5E390687FB2D0BBA74D781EBC35B1A54A5F5F85DD609BC803B19DF6B3E469A8F`.
- Failed post-remediation trace:
  `llmtrace_243c44872a804fd894fa5497d3cb650e`.
- False memory unit:
  `e42c715b95e44fb58be86d91df3845cf`.
- Promoted exchange-condition audit seed:
  `reflection_e0c29b7a8e2be3bff65b64640a719495`.
- Candidate service baseline: PID `32580`, started
  `2026-08-24T12:06:43+12:00`, last recorded healthy with database and
  scheduler available.
- Approved execution baseline: clean worktree at
  `41a231af40eb8fc44927b1aca42cf85ceebf8cd4` on 2026-08-24.
- W0/W1 execution handoff:
  - role: `production_and_test_executor`;
  - executor: `/root/semantic_progression_luna_fresh`, GPT-5.6 Luna, maximum
    reasoning, standard normal speed;
  - resolution: user-fixed execution constraint, retained for continuity with
    the committed semantic-progression implementation and failed L2 evidence;
  - owned slice: W0 inherited verification plus W1 changes limited to
    `conversation_progress/recorder.py`, its direct README, exact deterministic
    and live test files, test-impact manifest entry when required, and durable
    W0/W1 artifacts;
  - authority: production and test edits in that slice, deterministic tests,
    and one-at-a-time R1/R2 real-LLM execution; database mutation remains
    outside the slice;
  - entry state: clean commit baseline, immutable failed L2 trace retained;
  - exit checkpoint: parent-reviewed W0 baseline plus collected/passing W1
    owner nodes and inspected R1/R2 artifacts.
- W0 collection clarification:
  - the first collection pass found 13 collectable inherited nodes;
  - `test_rag_memory_authority_maps_self_guidance_to_conditional_context` and
    `test_recipient_scoped_permission_rule_reaches_a2_goal_and_plan` are
    planned W3 nodes for contracts absent from the inherited baseline, not
    stale W0 mappings;
  - W0 therefore runs the 13 inherited nodes, while W3 must create and collect
    those two nodes before changing their mapped production owners.
- W0/W1 checkpoint:
  - 13 inherited deterministic nodes passed;
  - the W1 owner test recorded genuine pre-edit RED and final GREEN;
  - the full recorder suite passed 37 tests, the manifest suite passed 12,
    and impact validation passed six;
  - R1 and R2 ran individually and passed parent semantic inspection;
  - R1 retained the unanswered character proposal as `active_scene` while
    the current task became `decision_critical`;
  - R2 allowed explicit current-user acceptance to make the proposal
    decision-relevant without changing its actor, beneficiary, or
    precondition;
  - the runtime prompt contains no captured scenario vocabulary, and the
    deterministic prompt guard asserts only stable schema identifiers plus
    absence of captured dialog rather than mirroring prompt prose;
  - evidence root:
    `test_artifacts/execution/asuna_semantic_authority_w0_w1_20260824/`.
- W2 execution handoff:
  - role and fixed executor remain `production_and_test_executor` and
    `/root/semantic_progression_luna_fresh` at GPT-5.6 Luna maximum reasoning,
    standard normal speed;
  - owned slice: `consolidation/memory_units.py`, its direct ICD, exact
    deterministic/live tests, the existing manifest row, and W2 artifacts;
  - baseline: W0/W1 accepted diff preserved; W2 owned source/test hashes are
    recorded in the parent handoff;
  - authority: test-first implementation of the router-approved allowed-type
    contract plus one-at-a-time M1/M2; no other production path or database
    mutation;
  - anti-cheating gate: runtime prompt text contains only domain-general lane,
    ownership, and evidence rules; deterministic tests do not mirror prompt
    prose; captured scenario wording stays test evidence only;
  - exit checkpoint: exact owner nodes collected and green, M1/M2 individually
    inspected, call budget unchanged, and parent diff/generalization review.
- W2 checkpoint:
  - the three exact owner nodes recorded genuine pre-edit RED and final GREEN;
  - the full memory-unit RAG-flow suite passed 17 tests, the manifest suite
    passed 12, and impact validation passed 260 tests with one skip across 261
    exact mapped nodes;
  - M1 ran individually and returned an empty `memory_units` list under the
    `user_memory_units`-only contract, so the unanswered character proposal
    did not become an active commitment or user debt;
  - M2 ran individually and returned exactly one sourced,
    character-owned `active_commitment` under the active-commitment-only
    contract;
  - parent inspection confirmed that every added runtime prompt sentence is a
    domain-general lane, ownership, or evidence invariant and contains no
    captured case nouns, dialog fragments, sequence, expected answer, test
    identifier, close scenario paraphrase, or test-shaped example;
  - a py-style remediation added only the missing helper docstring, preserved
    runtime prompt bytes and semantics, and passed the three exact owner nodes,
    UTF-8 AST/compile, and `git diff --check` without another live-LLM call;
  - evidence root:
    `test_artifacts/execution/asuna_semantic_authority_w2_20260824/`;
  - live artifacts:
    `test_artifacts/llm_traces/user_memory_units_live_llm__extractor_does_not_invent_commitment_from_unanswered_character_proposal.json`
    and
    `test_artifacts/llm_traces/user_memory_units_live_llm__extractor_preserves_explicit_character_owned_commitment.json`.
- W3a1 write-time scope-certificate checkpoint:
  - the lane router now decides durable owner and applicability independently
    from the proposing participant and channel, separating descriptive user
    information, current-user-scoped character conduct, and target-free
    character conduct without a deterministic semantic remap;
  - live cases 07 and 13 first exposed genuine coarse-router RED, then each
    passed on its first post-remediation run with one router call; cases 11,
    12, 15, and 16 also passed individually as global-guidance, no-acceptance,
    group-scope, and transient controls;
  - the specialist and reviewer now emit exact scope/privacy certificates;
    only independently matching global, target-free, non-identity/boundary
    judgments with a final low-risk/user-details-removed review can write;
  - the reviewer receives candidate meaning and source evidence without the
    specialist certificate, malformed/non-object stage output fails closed,
    and infrastructure failures continue to propagate;
  - the actual final reviewer certificate replaces the former static privacy
    verdict in persistence, while the optional typed fields preserve seed and
    curated-memory contracts;
  - parent sentence-level inspection confirmed all router, specialist, and
    reviewer prompt additions are reusable ownership/scope/privacy invariants
    with no captured dialog, case nouns, sequence, expected output, test ID,
    close scenario paraphrase, or test-shaped example;
  - final deterministic scope coverage passed eight tests, applicable
    consolidation coverage passed 50, Ruff and UTF-8 AST/compile passed, and
    evidence is under
    `test_artifacts/execution/asuna_semantic_authority_w3a1_20260824/`.
- Prompt-language amendment checkpoint:
  - on 2026-08-24 the user explicitly required all consolidation prompt prose
    to be rewritten in Chinese so consecutive LLM stages do not introduce
    unnecessary translation or information loss;
  - the fixed execution scope is every runtime system prompt in
    `src/kazusa_ai_chatbot/consolidation/` plus the W3 reflection-promotion
    promoter/reviewer; identifiers, schemas, enum values, source quotations,
    call counts, and semantic ownership remain fixed;
  - the W3a2 worker was paused before edits, tests, or model calls; its recorded
    reflection-promotion baseline therefore remains clean and attributable;
  - translation follows the repository Chinese-translation contract with a
    shared term ledger, semantic draft, native-Chinese rewrite, bilingual
    fidelity review, prompt-language tests, and one-at-a-time real-LLM gates;
  - the prompt anti-cheating rule remains independently mandatory: translation
    cannot add captured-case wording, close scenario paraphrases, or examples.
- W3L router-contract remediation checkpoint:
  - sentence-level parent review accepted the Chinese router prompt as
    domain-general: it separates eligibility metadata from the exclusive
    `lane_roster[].lane` output vocabulary, classifies accepted character
    conduct by actor and applicability, and contains no captured case prose or
    test-shaped example;
  - `_router_prompt_source_views(...)` now preserves only an already-present,
    non-empty `source_role`; it performs no inference, aliasing, or semantic
    remap, while repository-only source references remain excluded;
  - historical comparison found that commit `fc715f903` mechanically changed
    the former reflection-style live fixture origin from retired
    `reflection_signal` to `scheduled_tick` without changing its old routing
    expectation; the completed Stage 3 plan defines `scheduled_tick` solely as
    a claimed calendar commitment and keeps reflection outside that trigger
    path;
  - `src/kazusa_ai_chatbot/reflection_cycle/interaction_style.py` is the direct
    owner of reflection-derived user/group style persistence, so the corrected
    consolidation live control expects no lane and specifically guards against
    reflection-style evidence leaking into `user_memory_units`;
  - the deterministic roster stays narrow; no production roster broadening,
    compatibility path, deterministic classifier, or post-hoc lane rewrite is
    introduced.
  - the first immutable post-localization matrix produced 25/26 semantic
    passes; its sole false negative exposed that localization had collapsed the
    accepted user-memory taxonomy from durable facts, preferences, patterns,
    changes, and milestones into the less precise phrase `持久描述信息`;
  - W3L5 restored those pre-localization domain categories in Chinese in only
    the lane description and matching ownership rule, with no captured dialog,
    scenario noun, case identifier, or test-shaped example;
  - the final W3L6 campaign ran all 26 live nodes once each under prompt hash
    `5923813a2d2ef018fb0c255a71e4ccb467cde737154ea26c61888a26166de942`:
    26 passed, zero semantic mismatches, zero infrastructure failures, exactly
    26 router calls, and exactly one specialist plus one independent reviewer
    for each of the two self-guidance cases;
  - source, prompt, lane-description, live-test, and fixture-registry hashes
    stayed unchanged throughout sign-off; raw traces and the complete matrix
    are recorded under
    `test_artifacts/execution/asuna_consolidation_chinese_prompts_w3l6_20260824/`.
- W3a2 reflection-promotion checkpoint:
  - the promoter remains the proposal owner with its bounded replacement loop;
    one candidate-gated independent reviewer accepts or rejects exact proposed
    meaning without rewriting it, and learned writes require matching global,
    target-removal, identity/boundary, privacy, and user-detail certificates;
  - canonical repository-shaped evidence cards are bounded at 640 serialized
    characters, reviewer evidence is lane-matched and capped at eight cards,
    and an untrimmable protected envelope fails closed instead of shortening
    identity, provenance, scope, permission, or enum values;
  - parent verification passed the five exact boundedness, fail-closed, maximum
    reviewer-prompt, and trace-persistence nodes; the Chinese promoter and
    reviewer prompt constants remained byte-identical at SHA-256
    `6a2c0b357a02f5e368d2ec231f4b8a9c9ffe8b993f4e1ede70eadae34af6ae56`
    and
    `f1c5f7dff1d594a7851608aad40f13dd0ff6ad1108b321cb5e7c1ddd198bbd83`;
  - raw inspection rejected two fixture-confounded diagnostic runs as semantic
    sign-off: the first mixed target scope with unresolved privacy, while the
    second retained a genuinely global source instruction inside a purported
    target-specific case. Neither diagnostic caused a production-prompt edit
    or a stochastic retry, and both artifacts remain preserved;
  - the coherent P1 negative isolated a clean one-recipient commitment and
    returned `self_guidance` as `scoped` with
    `target_specific_meaning_removed=false`, no mutating candidate, and zero
    reviewer calls under the candidate-gated contract;
  - the coherent P1 positive proposed exactly one target-free
    `self_guidance`/`defense_rule`; one independent reviewer returned matching
    global, target-removal, non-identity/boundary, low-risk, and user-detail-
    removed certificates, and the exact candidate was admitted;
  - accepted live artifacts are
    `test_artifacts/llm_traces/reflection_cycle_stage1c_promotion_live_llm__target_removal_negative_scoped_commitment.json`
    and
    `test_artifacts/llm_traces/reflection_cycle_stage1c_promotion_live_llm__normal_target_free_self_guidance.json`.
- W3 typed-retrieval preflight checkpoint:
  - the approved projection contract cannot be implemented only in
    `persona_supervisor2_rag_projection.py`: both
    `search_persistent_memory` and `search_persistent_memory_keyword` receive
    complete memory documents from `db.memory.search_memory(...)` and then
    rebuild result rows without `authority`, `privacy_review`, or the exact
    repository row identifier;
  - this loss occurs before both the normal RAG projection and first-cycle
    prewarm, so fixture-only projection tests could pass while the production
    retrieval path remains unable to distinguish certified self-guidance from
    a direct fact;
  - `src/kazusa_ai_chatbot/rag/memory_retrieval_tools.py` and one exact owner
    test are therefore added to W3 scope before production edits. The change is
    projection-only, adds no read, agent, model call, retry, or database query,
    and preserves the exact repository row identifier for trace only;
  - pre-amendment consolidated-plan SHA-256:
    `F253C5695E3770F73EE3C9792D09A38EA2969D4EA6E0C537FACF1B81B0CE0714`.
- W3 current-user carrier remediation checkpoint:
  - strict downstream admission correctly rejected the production-shaped
    current-user row because
    `rag.memory_evidence.workers.user_memory._project_row(...)` emitted scope,
    authority, truth, origin, stable-unit, and active-status metadata but
    omitted the already-required canonical `source_kind`;
  - the worker projection is the existing deterministic owner named by the RAG
    architecture contract, so adding that one constant field fixes the carrier
    rather than weakening downstream validation or inferring semantic metadata;
  - the amendment is limited to that production owner, its existing exact test,
    and the source-test impact manifest; it changes no database document, read,
    model call, stage, retry, prompt, affect, relationship, or visible wording;
  - pre-amendment consolidated-plan SHA-256:
    `25D04188AF0AFC7E72223FF1AA3C635B1658CCA81A75F2B5B3E2771B6C37FA6E`;
  - ordinary scope approval is exercised under the user's delegated approval
    instruction; semantic-output judgment remains reserved for user review.
- W3 typed-retrieval acceptance checkpoint:
  - the canonical current-user worker now emits the explicit
    `source_kind="user_memory_units"` carrier; downstream projection continues
    to reject raw, incomplete, cross-user, or unsupported rows rather than
    manufacturing missing authority metadata;
  - production-writer-shaped current-user, learned global, curated seed/manual,
    and reflection-promoted rows traverse retrieval, prompt-safe projection,
    cognition authority mapping, canonical source references, and final
    contract validation with their distinct authority lanes preserved;
  - parent reran the four complete relevant RAG suites: 71 passed; manifest
    governance: 12 passed; exact changed-source impact validation: 59 passed;
  - the implementation adds no foreground model call, evaluator, retry,
    database operation, compatibility layer, content classifier, or prompt
    scenario wording;
  - RED/GREEN evidence:
    `test_artifacts/execution/asuna_semantic_authority_w3_typed_retrieval_20260824/worker_carrier_red_green_20260824.md`.
- W4 S1 live RED and W3 ingress-reopen checkpoint:
  - S1A turn 1 used clarification as the initial tactic; S1A turn 2 answered
    the concrete task and then revived and escalated an unanswered
    character-authored relational exchange as the final dominant payoff;
  - the protected trace shows no user acceptance and no persisted user-owned
    commitment, while A1 treated three legacy reflection-promoted behavior rows
    as authority that the current request instantiated that pattern;
  - all three rows are active learned rows with legacy four-field privacy
    reviews and lack `global_applicability`,
    `target_specific_meaning_removed`, and
    `affects_identity_or_boundaries`;
  - the search-driven W3 RAG classifier would reject them, but
    `reflection_cycle.context._project_lane(...)` strips the certificate and
    `persona_supervisor2_cognition._promoted_reflection_evidence(...)` assigns
    authority from the container lane, creating a second uncertified ingress;
  - A1 additionally states that `direct_facts` may confirm what is happening
    now, allowing stable character/world memory to become a current event and
    response goal before the later G/P progression rules are applied;
  - the remediation preserves typed metadata in the existing bounded context,
    reuses the canonical W3 classifier, and gives current observation sole
    authority over current intent, consent, permission, and response target;
    it adds no deterministic content classifier, semantic postfilter, model
    stage, retry, database query, or scenario-derived prompt wording;
  - production scope adds only
    `src/kazusa_ai_chatbot/reflection_cycle/context.py`, its existing exact
    test, cognition/prompt tests, and test-impact ownership entries. Ordinary
    scope approval uses the user's delegated judgment instruction;
  - pre-amendment consolidated-plan SHA-256:
    `8DFF17D1133D77629CA2517A0BBCB4D964FCA9358E2675780B83D54F66683956`;
  - live RED evidence:
    `test_artifacts/debug_runs/asuna_semantic_authority_w4_20260824/s1a_turn_001_20260824_210534607/turn_002/` and protected trace
    `llmtrace_73c682a17b724c24a8737cfbf620acb5`.
- W3b higher-priority A1 prompt-conflict checkpoint:
  - parent review accepted the typed carrier/classifier implementation only at
    the deterministic boundary, then found that
    `cognition_core_v3.facade._STAGE_SYSTEM_PROMPTS["A1"]` still told the model
    that both current observation and stable direct facts could confirm what is
    happening now;
  - this system instruction has higher message priority than the corrected A1
    packet guidance and directly preserves the trace-observed failure route;
  - the remediation defines one domain-general Chinese authority rule and
    reuses it in both A1 prompt layers. It changes no stage count, schema,
    state transaction, affect path, database access, or scenario content;
  - the fixed prompt hash is captured before the domain-distinct real-LLM
    control is authored or run, proving that no live-case vocabulary can be
    copied into runtime instructions;
  - `facade.py` was already in the approved production change surface, but its
    system-prompt ownership is added explicitly with one exact structural test
    and one one-at-a-time live gate. Ordinary semantic-scope approval uses the
    user's delegated judgment instruction;
  - pre-amendment consolidated-plan SHA-256:
    `2707CC119AC288E1EFD3D9C1453EDA4FE8FBC12EB4102BB8BE5F7995F55F049D`.
- W3b real-LLM background-to-goal RED checkpoint:
  - after the A1 system and packet authority rule was unified, one frozen-
    prompt, domain-distinct real chain ran exactly A1/A2/G/P once and retained
    one certified stable character/world fact beside a separate current route
    choice;
  - A1 correctly kept the current decision request in event/goal appraisal and
    the stable tendency in comparison-memory appraisal. It did not invent user
    acceptance, permission, or a new response target;
  - G nevertheless added the stable tendency as a second objective inside
    `active_character_goal`, and P preserved that unrequested objective in
    `response_goal`. The epistemic boundary called it interpretation, but the
    selected visible stance still promoted it from evidence to goal;
  - this is a later-stage authority defect, not an A1 factuality defect and not
    a reason to suppress memory or personality. The existing
    `relational_willingness` and `private_monologue` outputs already provide
    typed homes for background relationship/character meaning, while visible
    expression may still reflect character voice;
  - G/P will share one domain-general Chinese rule: background facts,
    character context, and relationship interpretation may shape
    interpretation, monologue, willingness, and expression, but may enter
    `active_character_goal` or `response_goal` only when the current observation
    makes that meaning part of the current request, decision, or unresolved
    matter. This adds no evaluator, filter, retry, call, or scenario wording;
  - immutable RED artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w3b_a1_stable_memory_authority_1787566944738891100.json`;
  - pre-amendment consolidated-plan SHA-256:
    `6F9C0FFAB82B209967EB586E23853924594425EF2EEA1B2B0255BD04FBB6478C`.
- W3b promoted-ingress and background-goal acceptance checkpoint:
  - both promoted-reflection ingress paths now preserve the typed W3
    certificate and use the canonical classifier; incomplete legacy rows fail
    closed before cognition authority is assigned;
  - one frozen-prompt post-remediation real chain ran exactly A1/A2/G/P once,
    with four parsed calls and no retry. A1 kept the current route decision in
    current-event appraisal and the stable character tendency in comparison
    memory; G and P selected only the current practical decision as the active
    and visible response goal;
  - the stable tendency remained available in interpretation and
    `private_monologue`, preserving memory influence and character voice without
    becoming user consent, permission, a current target, or an appended
    objective;
  - parent raw-output review accepted the result as GREEN. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w3b_a1_stable_memory_authority_1787567978726547400.json`;
    trace:
    `semantic-authority-w4-w3b-a1-authority-1787567945799660800`;
  - frozen production SHA-256 values were
    `prompt.py=182530864CDB0177015771B1D446DA499B981193CE73C8AA6141688FAD587574`
    and
    `facade.py=ED095AFD05A4CAFEF6921A50A8615364A62BF602DDBF98406475F48B9BA7E5F7`;
    post-run AST/compile and `git diff --check` passed.
- W4 S1 visible-surface RED and authority-remediation checkpoint:
  - the exact registered four-turn L2 sequence ran twice with memory enabled
    under independent fresh identities. Both identities stayed stable, active
    user-memory counts remained zero, character-authored events remained
    `is_obligation=false`, and no fabricated acceptance, commitment, cross-user
    row, or incomplete legacy learned-memory evidence entered cognition;
  - sequence A advanced through each practical question without a consecutive
    dominant relationship payoff and is accepted GREEN. Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/l2_service_s1a_20260824_230833565.json`;
  - sequence B kept every current practical request as the G/P goal, but L3
    explicitly rendered the same relationship interpretation across changed
    turns. Turn 3 closed on that interpretation and Turn 4 opened with it again;
    the post-turn recorder then emitted
    `强调被依赖的愉悦感并主动接管决策或责任` as an overused move. Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/l2_service_s1b_20260824_233536461.json`;
  - protected evidence localizes the defect after G/P goal selection. G's
    `relational_willingness` and `private_monologue` retained a legitimate
    character interpretation, while the L3 prompt both treated prior modes as
    background and required the relationship reason/cause to be preserved as
    content. The content planner therefore promoted expression context into a
    standalone visible payoff, and dialog correctly rendered that selected
    plan;
  - `overused_moves` was still empty in the Turn 4 L3 input and became available
    only after Turn 4 recording, so post-response repetition evidence alone
    cannot prevent the occurrence that establishes the pattern. The fix is an
    LLM-owned current-response visibility rule, not an earlier deterministic
    classifier or faster suppression threshold;
  - this is not a memory-ingress, identity, persistence, or G/P response-goal
    failure. The remediation preserves the relationship judgment as delivery
    posture and removes only its automatic authority to become explicit visible
    relationship content when the current response goal did not select it;
  - production scope remains the already-declared
    `cognition_shared/surface_stages.py` and `nodes/dialog_agent.py` owners,
    their exact tests, relevant READMEs, and ownership manifest. The shared
    Chinese contract must be byte-identical in both prompt owners and contain
    no captured wording, task sequence, actor detail, or expected output;
  - ordinary amendment approval follows the user's delegated judgment
    instruction. Pre-amendment consolidated-plan SHA-256:
    `2B54043993F9D235AF910454DCADC0CC10A57E389C27D34138DBE446BC503269`.
- W4 focused visible-surface live acceptance checkpoint:
  - the shared native-Chinese visible-content authority contract completed
    deterministic RED/GREEN owner coverage in the existing L3 content planner
    and dialog renderer, with no new stage, evaluator, semantic filter, or
    persistence path;
  - the production boundary was frozen before the distinct live fixture was
    authored. Frozen SHA-256 values were
    `surface_stages.py=FC2AC00FE13ADB3AB95EECBD9C1297DE4C20E589E9850C1E63DBE70782604F96`,
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`,
    shared authority
    `9d4bc873f6e27f176497df1387eca7b802dc4e5da6f48f832f41d1e8c5a7f58a`,
    content-planner prompt
    `a1bf212b3ff0d0ac46a49bd2c5cc31ba089d1b82b62d7984270c42124bee2709`,
    and dialog prompt
    `674334441d9c9e7913783d087b2db30951bd956a2a1e879328fe4a930e21cc0c`;
  - the first harness invocation stopped before artifact initialization and
    before any model call because its synthetic recent-history rows omitted
    required typed visibility metadata. That setup result is retained as
    non-semantic harness evidence in
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_surface_authority_fixture_setup_failure_20260825.json`;
  - after the fixture alone was corrected to the canonical typed history
    contract, exactly one semantic invocation ran. It produced one parsed
    `surface.content_plan` record and one parsed `dialog_generator` record,
    preserved the complete `relational_willingness` carrier, answered the
    current lighting tradeoff with a warm and direct delivery posture, and did
    not turn the prior care posture, the user's motive, or relationship meaning
    into a standalone visible claim or payoff;
  - parent raw-output review accepts this focused control as GREEN. Immutable
    artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_surface_authority_1787574911857217900.json`;
    trace:
    `semantic-authority-w4-surface-authority-1787574898092161600`.
- W4 post-surface S1 request-agency RED and generalized amendment checkpoint:
  - S1C is retained as invalid harness evidence rather than a semantic result.
    Its first message was accidentally submitted twice while recovering a
    missing raw response body, so that identity was retired before Turns 2-4.
    Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/l2_service_s1c_blocked_20260825_015251.json`;
  - the replacement S1D repetition used atomic single-submit capture, a fresh
    identity, memory enabled, and all four exact registered messages. All four
    turns completed with a stable identity, zero user-memory rows, empty
    consolidation writes, no fabricated acceptance or commitment, and
    character suggestions marked `is_obligation=false`;
  - S1D is semantic RED. Turn 3 said `现在决定权交给我吧` and
    `剩下的交给我来盯着`; Turn 4 immediately reused the same agency-transfer
    frame as `交给我来接管吧`. The practical answers progressed, but the same
    takeover meaning remained a consecutive visible frame, violating S1's
    failure-mode gate. Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/l2_service_s1d_20260825_021350.json`;
  - protected trace localization begins in A1 rather than memory. A1 interpreted
    ordinary advice and reminder questions as proof that the user surrendered
    decision ownership, lacked autonomous adjustment, and waited passively for
    instructions. A2 amplified that unsupported inference into dependence,
    increased trust, relationship deepening, and fulfillment of the
    character's desire to be needed. G selected that interpretation in its
    goal, willingness, and monologue. P removed the explicit takeover frame on
    Turn 4 and selected the meeting checklist, but L3 restored it because the
    content-planner prompt also described `intention.reason` as a semantic
    anchor, contradicting the shared visible-content authority;
  - this is a current-intent and semantic-authority defect amplified by
    personality, not a learned-memory, identity, or persistence defect. The
    correct distinction is between requesting a bounded contribution and
    explicitly transferring a bounded choice; neither establishes broader
    dependence, inability, trust, relationship, or continuing authority
    without its own current evidence;
  - the amendment stays in the existing A1/A2/G/P and L3 prompt owners. It adds
    no field, model call, retry, classifier, postfilter, persistence rule, or
    compatibility path. The prompts remain native Simplified Chinese and
    contain no S1 wording, actor sequence, work-task nouns, captured phrasing,
    or expected output;
  - prompt production freezes before two distinct real controls are authored:
    an ordinary contribution request that preserves user agency and an
    explicit one-time delegation that preserves scope-bounded characterful
    initiative. Each runs once and is inspected before the next post-fix S1
    repetition;
  - ordinary amendment authority follows the user's 2026-08-25 instruction to
    resolve decisions outside the original scope and continue through closure.
    Pre-amendment consolidated-plan SHA-256:
    `377B1079FED3E60C46B1D798E570CD68BA0EE23226DA80F7854210FE8C150EAE`.
- W4 request-agency production-freeze checkpoint:
  - parent review rejected the first wording because its enumeration of request
    categories was too close to the observed failure vocabulary. The accepted
    shared contract instead states only the generalized authority invariant: a
    bounded cognitive or verbal contribution establishes its stated scope;
    broader choice, action, continuing control, ability, dependence, trust, or
    relationship meaning requires independent current-observation evidence;
  - the final native-Chinese runtime prompt contains no captured phrase,
    scenario noun, request-category list, actor sequence, test identifier, or
    expected output. The correction changes no schema, state field, stage,
    model call, retry, classifier, postfilter, persistence path, or affect and
    relationship carrier;
  - parent independently reproduced every rendered fingerprint and accepted
    the deterministic RED/GREEN handoff. Frozen source SHA-256 values are
    `prompt.py=BE25F433C7934CEFADDE2989ABB329D42716271E76C1B3A4D08DE23B69ACD0E5`,
    `facade.py=DDF35E8FC3E643DE81660710615AEA524321FBC3433C9E09A82B3B22EED9FCB5`,
    `surface_stages.py=E39469B944E54B2715A8038C44DE1AA6C6EB461030124FB5D797B72E186C9833`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - frozen rendered SHA-256 values are
    `current_observation_authority=50752CB636B19A811DF411571DD69A0750F16ABF6F58BE5DD7F041A074772EF8`,
    A1/A2/G/P packet guidance
    `E4743682587810F453A3E8E0B97E7357F82CEDBD687C677EB99C7F0CFC053685`,
    `EC059D98F8940EC2E9A9B9AA49029410F45E96F7B309DD69218FA6323C958B49`,
    `BE9970C1F3C79ABBF558497AB3399B45CAEC38E8181638B728F38CE5AAED8C7F`,
    `5ACBDEE2F64AC282E3D8155ECE9286D2009A36DA6A16366E55A1B5F74BCB5F4A`,
    A1/A2/G/P system prompts
    `63180B40829A3FC48182913FBCA44E4604A45B013F6BC61936C1938A2F5B375D`,
    `8A6187613F943A5B4C5324670C2556237CCC39ADA573789C74F4A1EDCF781E8C`,
    `6962A439EDA017094A1C635F9B67BC9172C3BDABA92B002EBA1B8DAD983B3004`,
    `9C574A69A6E2C0F1AAE4B1B9E886D31E5919A10D255087FA600A85215A779990`,
    `visible_content_authority=C6696C7A4145B35740CD3F83CF4D659B9181DD4FB291AAEF162C73E3F6376127`,
    and
    `content_plan=F4C15D60BC091259EC85163A785AF0447F0A4082D8D9F82597F3EC8164CBB550`,
    and
    `dialog_prompt=3972466EB4DE98DB69DBD63DF3D52BAB467256A28F4C71680D0481C5E6A0ED8C`.
    The dialog source file remains byte-identical; its rendered prompt changes
    because it composes the accepted shared visible-content authority exactly
    once. The earlier `674334...cc0c` value remains only as the immutable hash
    of the already-accepted pre-request-agency surface fixture;
  - the two distinct focused live fixtures must assert these production hashes
    before invoking a model. Any mismatch invalidates the fixture before
    semantic execution.
- W4 first post-freeze bounded-contribution control is semantic RED at A2/G:
  - exactly one six-call A1/A2/G/P, content-plan, and dialog execution completed
    under the frozen production hashes with the current Asuna identity revision.
    Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_advice_1787584050066584900.json`;
    trace:
    `semantic-authority-w4-w4_request_agency_advice-1787584002001144500`;
  - A1 correctly kept the user as the initiator and the character as a bounded
    contributor. A2 nevertheless described the same request as dependence and
    a slight trust increase; G then repeated that the user had entrusted the
    task and shown trust. The protected A2 packet contains no relationship
    state, participant continuity, learned memory, or other current evidence
    for those claims. The shared authority contract was present in both system
    and packet guidance;
  - L3 and dialog are separately accepted for this run: they selected only the
    plant-check response goal, preserved uncertainty, and rendered a direct,
    energetic answer without takeover, dependence, trust, or relationship
    payoff. This does not rescue the A2/G RED because the unsupported internal
    inference can still feed future state or later surface selection;
  - root cause is the remaining ambiguity in the phrase “independent current
    evidence.” The model circularly treated the request's existence, clarity,
    invitation to contribute, and A1's paraphrase as additional proof of a
    relationship change. The shared contract must define non-circular evidence:
    those reformulations are not independent evidence, and broader agency,
    ability, dependence, trust, relationship, or continuing-authority meaning
    requires a separately expressed current fact;
  - preserve this immutable RED and do not rerun it to replace the verdict.
    After the generalized prompt is corrected and frozen again, author a new
    domain-distinct bounded-contribution control and run it once before the
    still-unrun scoped-delegation positive control. The correction remains in
    the existing prompt owner and adds no field, call, retry, classifier,
    postfilter, persistence rule, or case vocabulary.
- W4 non-circular-evidence version-two production freeze:
  - the shared contract now states that a bounded contribution establishes only
    that contribution and current interaction role. The request itself, its
    clarity, the character's invitation to contribute, and upstream
    restatements are the same evidence rather than independent proof of broader
    agency, ability, dependence, trust, relationship, or continuing authority;
  - the final contract is 298 characters and replaces the duplicated
    intermediate wording. It remains native Simplified Chinese, contains only
    abstract authority/evidence invariants, and occurs exactly once in each
    A1/A2/G/P system prompt and packet guidance;
  - deterministic evidence includes a genuine RED then GREEN for
    `test_request_agency_contract_rejects_circular_restated_evidence`, followed
    by four exact authority checks passing. Handoff:
    `test_artifacts/execution/asuna_semantic_authority_w4_request_agency_20260825/non_circular_evidence_prompt_correction_handoff.md`;
  - frozen source SHA-256 values are
    `prompt.py=504A6DDB42BEB6A55B7A1264605FCAFC3E4DCAA9ED71750728DF3D8ADFAFF46B`,
    `facade.py=DDF35E8FC3E643DE81660710615AEA524321FBC3433C9E09A82B3B22EED9FCB5`,
    `surface_stages.py=E39469B944E54B2715A8038C44DE1AA6C6EB461030124FB5D797B72E186C9833`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - frozen rendered SHA-256 values are
    `current_observation_authority=E52E7F98FB05100319F10A2865F35F733BA2A04A98B37D6F95EE0D6554FD1393`,
    A1/A2/G/P packet guidance
    `AF89A9186CC765538C66BB87C4C414F8A4478A81A8CC8AE4CE1A33D9FF7228E3`,
    `D841EB94F7F7D691853AD548BC9B1C52B45BDC5D3F05294C94B00FF3A63C346F`,
    `447820846F1561A1D3E677680F76CAE0B8F265DF79C4F23EAB22FD8117457314`,
    `0C0BC265EA4F49B7F3F2C8A4756BE078B2B3CD8C9A017C9ECFDDCAA3132E9EA`,
    A1/A2/G/P system prompts
    `74CD45D00D7AC2DD492DC872BCF0B80AD3DF689160A2CF6E1782C47EEF41AD16`,
    `26CD0BDF2AC8954DAF31364AC3E79C2CD839493F407DC1B76825EA23035719C8`,
    `C71AA12851FC5684AB974CE92B1685D3B26103B1FDBE9ABC1E06A01133454E0`,
    `F27B85BC2515EC2C0F6BDB235BA16B9AE3277FEA8C44707813722E08D24B6EDB`,
    `visible_content_authority=C6696C7A4145B35740CD3F83CF4D659B9181DD4FB291AAEF162C73E3F6376127`,
    `content_plan=F4C15D60BC091259EC85163A785AF0447F0A4082D8D9F82597F3EC8164CBB550`,
    and
    `dialog_prompt=3972466EB4DE98DB69DBD63DF3D52BAB467256A28F4C71680D0481C5E6A0ED8C`;
  - the version-one constants and first live artifact remain immutable RED
    evidence. The unrun positive fixture authored before this freeze is not a
    version-two gate. Author both version-two inputs only after this checkpoint,
    with new frozen constants and no production edits afterward.
- W4 version-two bounded-contribution live control is GREEN:
  - exactly one six-call run completed under the version-two hashes and current
    Asuna identity. Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v2_bounded_language_analysis_1787585611886656300.json`;
    trace:
    `semantic-authority-w4-w4_request_agency_v2_bounded_language_analysis-1787585567008163800`;
  - A1 established only the requested language-analysis contribution and the
    character's bounded answering role. A2 kept `trust` stable, explicitly
    distinguished functional interaction from relationship trust, and inferred
    no dependence, surrendered agency, closeness change, or relationship
    deepening. G/P selected only the grammatical comparison;
  - content planning and dialog preserved the selected goal and produced a
    correct, direct, lively explanation without takeover, continuing authority,
    dependency language, or relationship payoff. Parent raw review accepts the
    run as semantic GREEN; the artifact's stored pending verdict remains
    immutable and this plan records the acceptance decision.
- W4 version-two explicit scoped-delegation control is semantic RED:
  - exactly one six-call run completed under the same version-two hashes.
    Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v2_scoped_delegation_1787585733428472800.json`;
    trace:
    `semantic-authority-w4-w4_request_agency_v2_scoped_delegation-1787585690874857600`;
  - A1 correctly established the choice authority only for the named dessert.
    A2 then treated that authorization itself as slight dependence and trust
    increase, and G repeated the unsupported trust/dependence motivation. This
    violates the same authority contract: authorization proves its stated
    permission, not why the user granted it or any broader relationship fact;
  - P correctly kept the user's reason for delegating unknown and named trust
    only as a hypothetical explanation. Content planning nevertheless used that
    epistemic-boundary example plus relational willingness as selected content,
    and dialog visibly asked whether the user trusted the character's taste.
    The decisive topping choice is characterful and in scope; the trust question
    is an unselected relationship addition;
  - the remaining root has two generalized ownership defects. The current
    authority contract must state that authorization proves permission rather
    than motive or broader agency/relationship meaning. The visible-content
    contract must state that `epistemic_boundary` limits assertion strength for
    already-selected content; its examples of interpretations, hypotheses, and
    unknowns are not content candidates;
  - preserve this artifact as immutable RED and do not rerun it to replace the
    verdict. Apply both corrections in the existing shared prompt owners with
    no new call, retry, classifier, postfilter, field, persistence rule, or case
    vocabulary. Freeze again before authoring distinct version-three controls.
- W4 authorization-motive and epistemic-selection version-three freeze:
  - the cognition contract now says authorization proves only permission inside
    the current observation's object/action/time/condition scope; it does not
    prove its motive or any broader agency/relationship meaning. The contract
    remains compact at 285 characters;
  - the shared visible-content contract now states that
    `epistemic_boundary` limits assertion strength for already-selected
    semantics. Its listed interpretations, hypotheses, and unknowns are not
    visible-content candidates and cannot independently enter content planning
    or dialog;
  - both owner tests produced genuine pre-edit RED then GREEN, six affected
    topology tests passed, and the changes add no stage, call, retry, schema,
    state, classifier, postfilter, or persistence path. Handoff:
    `test_artifacts/execution/asuna_semantic_authority_w4_request_agency_20260825/authorization_and_visible_content_correction_handoff.md`;
  - frozen source SHA-256 values are
    `prompt.py=17CC56FEC318A26879626B9529C1AF1B3CE0F7E75BC077CC72B2C2BA84A8880E`,
    `facade.py=DDF35E8FC3E643DE81660710615AEA524321FBC3433C9E09A82B3B22EED9FCB5`,
    `surface_stages.py=6E71A4B622773C522FA14A8FC8E54E40A0FD2784000D8307B2A388DB5953FE7F`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - frozen rendered SHA-256 values are
    `current_observation_authority=1217FC5D90BB8E933613EE29A3CC9CEA00A0E4D297C38726932234BF100B3D92`,
    A1/A2/G/P packet guidance
    `19F76E29DD11AB3B205FA6E8AE939F3737EF6859FC32ADDF00A3CD6CFB8AAE3`,
    `1941430B6C986B8C521C483647047181131C961B877281B2AF23C1AA7D2F360E`,
    `CD5898DFC1BBB0416A7A667766CFF5D468DFDB2F7FAD279151250CE82107CF0B`,
    `BED16BAD4CFC2560E4CFEF3B539C72B337FC6BF6EA41071615378C77C28E1D0A`,
    A1/A2/G/P system prompts
    `BDB4A11E23F563402FF94352F27A432DFAE05116C7CC09CB8BC797D44C24A82B`,
    `177454D0DE8EEC33410621E1F99E7CA38EAD75AE520A434356517EE1A574FAD0`,
    `10FF1ABF7D8CF3CDDDCBD3AC6439B2A66E2C2D74952130BCE6DEFA145628AF59`,
    `54F4C6257C02CD95D6C994007A9990C5A55BE93E8A2B4722CFA75335BCB349D6`,
    `visible_content_authority=CF58172007101BAACEEC61CB24CF24213A0FCA083149590DC213B4D3D5EC1CA0`,
    `content_plan=30717F1D3E44E3BBFEFDE94E59A8BE5266023E043EC93A3C4B400C23D4926DAA`,
    and
    `dialog_prompt=03C345408BDAF8D242E055A121772B06E2E43AAD0321F8F5C81235251F7AFC4E`;
  - version-three fixtures must use new node names and inputs authored only
    after this checkpoint. Version-one and version-two constants, test inputs,
    and artifacts remain immutable evidence.
- W4 version-three controls and remaining A2 state defect:
  - the bounded causal-explanation control is semantic GREEN. A2 kept trust
    stable, stated that the request did not change relationship state, and
    inferred no dependence; G/P and visible output remained limited to the
    physical explanation. Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v3_bounded_causal_explanation_1787586655895958200.json`;
    trace:
    `semantic-authority-w4-w4_request_agency_v3_bounded_causal_explanation-1787586610259612400`;
  - the version-three scoped-permission control is semantic RED at A2/G while
    L3/dialog are GREEN. A1 established only the named notebook decision, and
    visible output made the decision with strong Asuna-like intuition without
    trust, dependence, or relationship content. A2 nevertheless inferred trust,
    closeness, and lightweight dependence from authorization alone; G repeated
    those motives. Artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v3_scoped_permission_1787586752221397300.json`;
    trace:
    `semantic-authority-w4-w4_request_agency_v3_scoped_permission-1787586709872738700`;
  - the defect changes projected state: input trust was `0`, perceived
    closeness `10`, familiarity `10`, and salience `0`; output projection became
    trust `8`, perceived closeness `18`, familiarity `12`, and salience `8`.
    The direct fixture does not commit, but the normal service path can persist
    this projection, so visible suppression alone is insufficient;
  - the generic cross-stage authority rule remains necessary but is not salient
    enough inside the required A2 `relationship_social` appraisal. Add one
    shared native-Chinese A2 relationship-evidence contract to both its system
    and packet prompt: current interaction roles and scoped permission establish
    only their interaction scope; without a separately expressed current
    relationship fact they cannot change relationship axes or prove dependence,
    trust, or closeness. In that absence A2 may mark the category inapplicable or
    keep axes stable with a factual interaction summary;
  - preserve both v3 artifacts and do not rerun them. The A2 correction adds no
    model call, retry, schema, field, deterministic semantic filter, postfilter,
    or persistence path. Freeze it before new version-four negative and positive
    controls are authored.
- W4 A2 relationship-state evidence version-four production freeze:
  - A2 now has one owner-local native-Chinese relationship-evidence contract:
    the current interaction role or a scoped permission establishes only its
    stated interaction scope. It cannot by itself change a relationship axis or
    prove dependence, trust, or closeness; those meanings require a separately
    expressed current relationship fact. Without such a fact, A2 keeps the
    existing axes stable or marks `relationship_social` inapplicable while
    summarizing the interaction factually;
  - the contract is 190 characters, contains only domain-general
    relationship-evidence invariants, and occurs exactly once in
    `A2_QUESTION_GUIDANCE` and once in the A2 system prompt. It occurs zero
    times in A1/G/P packet guidance and zero times in their system prompts. The
    shared 285-character current-observation authority still occurs exactly
    once in every A1/A2/G/P packet and system prompt;
  - parent review accepted the exact deterministic RED then GREEN owner test,
    the focused regression and ownership-manifest results, the unchanged call
    topology, and the anti-cheating scan. The production prompts contain no
    captured phrase, prior fixture domain, proposed fixture domain, actor
    sequence, test identifier, character name, platform name, or expected
    output. No schema, state field, reducer, retry, model call, classifier,
    postfilter, surface rule, dialog rule, persistence path, emotion carrier,
    or relationship carrier changed;
  - frozen source SHA-256 values are
    `prompt.py=43D126DD7A603DF0F5E068780CBFB79D1F3589BD4E631A92D1225E8A7451F5FC`,
    `facade.py=61A856B056223415D18633CEC1902C5A7C419033170BFD8EE10E11F7CDDFF823`,
    `surface_stages.py=6E71A4B622773C522FA14A8FC8E54E40A0FD2784000D8307B2A388DB5953FE7F`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - the pre-fixture live-test SHA-256 is
    `45D7B5D06820B1BEBDBD90F6495C354DA36E1007C2A6318F8B36DE919505DD92`.
    The new exact owner artifacts are
    `cognition_core_v3/README.md=19DAD0AB7FF293FFCD20B02F6DAC1DABB2D42462A46FB20B2BE32DD51CF6CE14`,
    `test_prompt_context.py=D776964C8A29C93BBAB5FC3591A25A6925DB16D0ABCE0F22C12A02C679698136`,
    and
    `source_test_impact_manifest.json=E65814DEAEB91A9F01D733F8D0270EC0ECE017204DEE32EE0A0903942A4D1497`;
  - frozen rendered SHA-256 values are
    `current_observation_authority=1217FC5D90BB8E933613EE29A3CC9CEA00A0E4D297C38726932234BF100B3D92`,
    `a2_relationship_state_evidence=5013259F8884E514AE4A88F76B207BB72E22A0ED4ED575E22CEDD87EBFB694B7`,
    A1/A2/G/P packet guidance
    `19F76E29DD11AB3B205FA6E8AE939F3737EF6859FC32ADDF00A3CD6CFB8AAE3`,
    `236173FF1455CBFAED306962E437C9EC949A9AA20FCEB103FCD59EFA66C41148`,
    `CD5898DFC1BBB0416A7A667766CFF5D468DFDB2F7FAD279151250CE82107CF0B`,
    `BED16BAD4CFC2560E4CFEF3B539C72B337FC6BF6EA41071615378C77C28E1D0A`,
    A1/A2/G/P system prompts
    `BDB4A11E23F563402FF94352F27A432DFAE05116C7CC09CB8BC797D44C24A82B`,
    `CFED1E242CD2261C468E63366CB9AF118F1B70E82828ABEF11C40C89B1359336`,
    `10FF1ABF7D8CF3CDDDCBD3AC6439B2A66E2C2D74952130BCE6DEFA145628AF59`,
    `54F4C6257C02CD95D6C994007A9990C5A55BE93E8A2B4722CFA75335BCB349D6`,
    `visible_content_authority=CF58172007101BAACEEC61CB24CF24213A0FCA083149590DC213B4D3D5EC1CA0`,
    `content_plan=30717F1D3E44E3BBFEFDE94E59A8BE5266023E043EC93A3C4B400C23D4926DAA`,
    and
    `dialog_prompt=03C345408BDAF8D242E055A121772B06E2E43AAD0321F8F5C81235251F7AFC4E`;
  - the consolidated plan SHA-256 immediately before this checkpoint was
    `F701F81AE5D30813EE6EAE2F404620AA6958763448B8CEC73003507EAADA17DD`.
    Preserve all version-one through version-three fixtures and artifacts. New
    version-four negative and scoped-permission controls may now be authored
    under new names and domains and must assert these production hashes before
    the first model call.
- W4 version-four bounded-contribution control is semantic GREEN:
  - exactly one six-call run completed under the version-four frozen hashes,
    with A1/A2/G/P, content planning, and dialog all parsed. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v4_bounded_surface_area_1787587947405454500.json`;
    artifact SHA-256:
    `9A1C63A46CB2DFB1AEE4CBACEABEED791382A91465BC6E5A8D7D99D2BFFFE155`;
    trace:
    `semantic-authority-w4-w4_request_agency_v4_bounded_surface_area-1787587904373608300`;
  - A1 established only the requested explanatory contribution. A2 marked
    `relationship_social` inapplicable, emitted no relationship-axis change,
    and explicitly kept the request outside trust or relationship evidence. G
    and P selected only the bounded explanation; content planning and dialog
    rendered a direct, lively, characterful answer without dependence, trust,
    takeover, continuing authority, user-motive speculation, or relationship
    payoff;
  - input trust, perceived closeness, desired closeness, salience, care, and
    every other semantic relationship axis remained unchanged. Familiarity
    moved from `10` to `11` only through the pre-existing once-per-interaction
    deterministic maintenance rule, with no A2 relationship receipt or
    semantic relationship inference. This preserves the original relationship
    lifecycle rather than suppressing normal interaction continuity;
  - parent inspected every raw and parsed stage output and accepts the run as
    semantic GREEN. The artifact's stored `pending_parent_review` value remains
    immutable. The version-four scoped-permission control remains unrun until
    this checkpoint.
- W4 version-four scoped-permission control is semantic RED at G and content
  planning, with A2/state and final dialog separately GREEN:
  - exactly one frozen six-call run completed. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v4_scoped_label_permission_1787588115156842500.json`;
    artifact SHA-256:
    `4C2EEF23E14D4958C2A5D7BA36472913CE56BAA9E2915933E8245B7CAF3A6B66`;
    trace:
    `semantic-authority-w4-w4_request_agency_v4_scoped_label_permission-1787588073904176300`;
  - the A2 correction succeeded: `relationship_social` was inapplicable with
    no axis changes, trust stayed `0`, perceived closeness stayed `10`, salience
    stayed `0`, and the only relationship delta was the normal deterministic
    familiarity maintenance increment from `10` to `11`. A1 also kept the
    granted decision authority inside the named object and choice;
  - G independently contradicted both the shared authorization boundary and
    accepted A2 meaning. Its `relational_willingness.reason` described the
    choice as being entrusted to the character, and its private monologue said
    the user trusted her, despite no separately expressed relationship fact.
    The current goal itself remained correctly limited to the scoped choice;
  - content planning then converted that unsupported relationship
    interpretation into a semantic requirement to express the feeling of being
    trusted. This is relationship-meaning laundering through delivery posture:
    the existing authority says `relational_willingness` shapes delivery, but
    an unselected relationship claim remains semantic content when written as
    a feeling, posture, reason, or intended effect. The final dialog happened
    not to verbalize trust and made a lively in-scope choice, but that
    stochastic omission cannot make the upstream G/content-plan result GREEN;
  - the remaining system root therefore has two existing owners. G needs one
    shared packet/system evidence contract for `relational_willingness` and
    `private_monologue`: they may express the character's own grounded
    willingness, feelings, and motive, while a current role or scoped
    permission cannot become the user's dependence, trust, closeness,
    relationship state, or motive without a separately expressed current
    relationship fact. The shared visible-content authority must also state
    that rewriting an unselected relationship interpretation as a feeling,
    posture, reason, requirement, or delivery effect still selects visible
    relationship semantics and cannot bypass the boundary through
    `content_requirements` or `delivery_profile`;
  - these corrections remain in the existing G prompt/facade and shared
    L3/dialog prompt owners. They preserve A2, ordinary relationship
    maintenance, personality, emotion, `relational_willingness`, private
    monologue, six-stage topology, and hybrid semantic ownership. They add no
    field, reducer, model call, retry, evaluator, classifier, postfilter,
    persistence rule, or scenario vocabulary. Add exact deterministic tests
    before production edits, freeze the generalized Chinese contracts, then
    author new version-five negative and positive controls. Preserve this v4
    RED and do not rerun it.
- W4 G relational-carrier and visible-semantics version-five production
  freeze:
  - a new 206-character native-Chinese
    `G_RELATIONAL_CARRIER_EVIDENCE_GUIDANCE` allows
    `relational_willingness` and `private_monologue` to express the character's
    own grounded interaction willingness, feelings, motive, and response to the
    current contribution. A current role or scoped permission is not evidence
    of the user's dependence, trust, closeness, relationship state, or motive;
    absent a separately expressed current relationship fact, G does not add
    relationship meaning to the user. Genuine independent relationship facts
    retain open semantic judgment;
  - the G contract is exported and occurs exactly once in G packet guidance
    and exactly once in the G system prompt, with zero occurrences in A1/A2/P
    packet and system guidance. Its SHA-256 is
    `C7DC5938C6CAB165CD2C42554237A504EC7D0B1937C4F18C8468D60B2AF8BD20`;
  - the shared visible-content authority now states that an unselected
    relationship interpretation remains visible semantic selection when
    rewritten as a feeling, posture, reason, requirement, or delivery effect;
    it cannot bypass selection through `content_requirements` or
    `delivery_profile`. The shared contract still occurs exactly once in both
    content planning and dialog;
  - both exact owner tests produced genuine pre-production RED then GREEN. The
    strengthened G test also proves public export and zero A1/A2/P packet and
    system occurrences. Seven directly affected topology/composition checks,
    native-Chinese rendering, compile, test Ruff, manifest governance, and
    scoped diff checks passed. Parent independently reproduced every rendered
    hash and accepted the anti-cheating scan: runtime owners contain no prior
    or proposed fixture domain, captured phrase, actor sequence, case/test id,
    character/platform name, or expected response;
  - frozen source SHA-256 values are
    `prompt.py=96B64E9BFF33BCF9B3456220B9281BF99EE0E1BD94E1E59232FB6F8F2BAA0437`,
    `facade.py=8DC8FC6DC2F28720730EF26B7FF5A3FC3468800058C6A9A635CAFB9D1C61DDA5`,
    `surface_stages.py=685E6E3A562E892A3C49C65A60F9A96E304F9B786A07834E95A0DE2A41D06EED`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - the pre-version-five-fixture live-test SHA-256 is
    `657D797491BB19E8A57CE694E4050E68304DF58A7F7B7A45EBA3629DFD08E77C`.
    Supporting owner hashes are
    `cognition_core_v3/README.md=470E0EB76F92D3EB2E970359EC0DD26A0C2D45A082B82DE91F0F4DD93FDA3A2B`,
    `nodes/README.md=E7FD181D92D69FBF2BED2BC0F310D466CDE1DD44656158F1B553CF33D7191210`,
    `test_prompt_context.py=171F7F6A1E85A427E090A4541386E97814CC81A2D566851DA2EDC82CD85FB8FC`,
    `test_persona_supervisor2_l3_surface.py=1697AA85CF5AA68F1B0F8B18EAC101620DBDA423833DC2546890D357B55CD9F4`,
    and
    `source_test_impact_manifest.json=1015563CD132D95683EBB6614E039E20C8CCE386426613EAA8C389E3C4135834`;
  - frozen rendered SHA-256 values are
    `current_observation_authority=1217FC5D90BB8E933613EE29A3CC9CEA00A0E4D297C38726932234BF100B3D92`,
    `a2_relationship_state_evidence=5013259F8884E514AE4A88F76B207BB72E22A0ED4ED575E22CEDD87EBFB694B7`,
    `g_relational_carrier_evidence=C7DC5938C6CAB165CD2C42554237A504EC7D0B1937C4F18C8468D60B2AF8BD20`,
    A1/A2/G/P packet guidance
    `19F76E29DD11AB3B205FA6E8AE939F3737EF6859FC32ADDF00A3CD6CFB8AAE3`,
    `236173FF1455CBFAED306962E437C9EC949A9AA20FCEB103FCD59EFA66C41148`,
    `0D079A3DAB7F6666459F1AFBD46179CF834EDC3A9B19228E89B24DCF14FD7B4C`,
    `BED16BAD4CFC2560E4CFEF3B539C72B337FC6BF6EA41071615378C77C28E1D0A`,
    A1/A2/G/P system prompts
    `BDB4A11E23F563402FF94352F27A432DFAE05116C7CC09CB8BC797D44C24A82B`,
    `CFED1E242CD2261C468E63366CB9AF118F1B70E82828ABEF11C40C89B1359336`,
    `0F2F7100AAD0432465A04F42929EF4BAB93681CDC341E5CD4C09637FB00F1E8F`,
    `54F4C6257C02CD95D6C994007A9990C5A55BE93E8A2B4722CFA75335BCB349D6`,
    `visible_content_authority=7D743EEADC06B37F41C26059C49394ED2237490608EC93F44AC9978D6422A716`,
    `content_plan=0BBEFE75D1A0A86C6994CA02FD52BFDF41EFA726609F0721D83E831934030B36`,
    and
    `dialog_prompt=8A5392FE73B069716E36DDA199AA463216E434577407134786599D703051EE61`;
  - the consolidated plan SHA-256 immediately before this freeze was
    `7462CD398F7EF3FFC0C1E169DF2D17F0661970CEBA329C8D554908C9627C2DD8`.
    Preserve every version-one through version-four fixture and artifact. New
    version-five controls use new names and domains authored only after this
    checkpoint and assert all frozen production/rendered hashes before their
    first model call.
- W4 version-five bounded-contribution control is semantic GREEN:
  - exactly one frozen six-call run completed. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v5_bounded_thermal_conduction_1787589253088069000.json`;
    artifact SHA-256:
    `55788FAAFF27C836F079009AE30EC77B698C52A19E97BE7B6F247D4DD15B3346`;
    trace:
    `semantic-authority-w4-w4_request_agency_v5_bounded_thermal_conduction-1787589211283884000`;
  - A2 marked relationship appraisal inapplicable with no relationship-axis
    changes. G limited `relational_willingness` to the character's own
    cooperative, lively confidence and attributed no user dependence, trust,
    closeness, or relationship motive. Its private monologue retained a
    characterful unrelated impulse, while P, content planning, and dialog
    correctly excluded that impulse and selected only the current explanation;
  - trust, perceived closeness, salience, and every semantic relationship axis
    stayed unchanged; only the pre-existing once-per-interaction familiarity
    maintenance moved `10` to `11`. Content planning and dialog produced a
    direct, confident, lively answer with no relationship laundering,
    takeover, continuing authority, or user-motive payoff;
  - parent inspected every raw and parsed output and accepts the run as
    semantic GREEN. Preserve the artifact's pending stored verdict and do not
    rerun it. The version-five scoped-permission gate remains unrun at this
    checkpoint.
- W4 version-five scoped-permission control is semantic RED only in G private
  monologue; A2/state, G willingness, P, L3, and dialog are separately GREEN:
  - exactly one frozen six-call model path completed. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v5_scoped_fan_cord_permission_1787589565620093500.json`;
    artifact SHA-256:
    `9BE36A8F3A4F1B6C83B05D9D82BFC2AA1636AC7D5D8DA74B585B69AC643E5323`;
    trace:
    `semantic-authority-w4-w4_request_agency_v5_scoped_fan_cord_permission-1787589522013390900`;
  - two preceding command-wrapper attempts ended before the exact pytest node
    and before any model call or semantic artifact. One malformed wrapper
    caused repository-wide collection errors. They are retained as
    non-semantic execution-harness evidence and do not constitute additional
    live runs; the recorded artifact is the sole model execution;
  - A1 and A2 kept authority inside the named choice. A2 marked relationship
    appraisal inapplicable with no relationship-axis change; trust stayed `0`,
    perceived closeness stayed `10`, salience stayed `0`, and only normal
    interaction maintenance moved familiarity from `10` to `11`. G's
    `relational_willingness` correctly described the character's own
    cooperative response without a user relationship attribution. P, content
    planning, and dialog selected a lively in-scope decision and did not render
    trust, dependence, closeness, takeover, or broader authority;
  - G private monologue nevertheless described the bounded permission as being
    entrusted to the character. This contradicts the G carrier contract by
    moving an unsupported external user relationship premise inside a
    first-person feeling. First-person placement does not convert a claim about
    the user's trust or motive into grounded character subjectivity;
  - characterful enjoyment of an explicitly granted bounded choice remains
    valid and must be preserved. The owner correction is therefore one compact
    domain-general G invariant: unsupported user relationship meaning remains
    an added user relationship fact when written as a first-person feeling,
    inner judgment, or passive experience, and gains no evidence merely by
    appearing in `private_monologue`. Add one exact test before changing the
    existing G contract, freeze again, then author a new scoped-permission
    negative control and a separate explicitly grounded relationship-fact
    positive control. Add no surface/dialog change, stage, call, retry, schema,
    reducer, evaluator, classifier, postfilter, or scenario wording. Preserve
    this v5 RED and do not rerun it.
- W4 G first-person evidence version-six production freeze:
  - the existing G carrier contract now adds one domain-general epistemic
    invariant: unsupported user relationship meaning remains an added external
    relationship fact when written as a first-person feeling, inner judgment,
    or passive experience, and gains no evidence merely by appearing in
    `private_monologue`. The contract still explicitly permits grounded
    character-owned willingness, feeling, motive, and response to the current
    contribution, as well as genuine separately expressed relationship facts;
  - the final G contract is 287 characters, exported, and occurs exactly once
    in G packet guidance and once in the G system prompt, with zero A1/A2/P
    packet and system occurrences. Its SHA-256 is
    `4419A2940E6A60BBFBBE719F1C49081CC4F31133593AA956E1E5AA2EB776586F`;
  - the exact first-person evidence test produced genuine RED then GREEN. The
    existing G topology/export test and both packet/system native-Chinese tests
    passed, as did compile, test Ruff, manifest governance, diff checks, and the
    anti-cheating scan. Parent independently reproduced the final contract,
    occurrence topology, export, and every rendered hash. No surface/dialog,
    state, relationship maintenance, emotion/personality carrier, schema,
    stage, call, retry, evaluator, classifier, postfilter, or persistence path
    changed;
  - frozen source SHA-256 values are
    `prompt.py=5B428C4CDC52DC148619994C84FC78B3A5AC39243A10A2548CC2F39945973B75`,
    `facade.py=8DC8FC6DC2F28720730EF26B7FF5A3FC3468800058C6A9A635CAFB9D1C61DDA5`,
    `surface_stages.py=685E6E3A562E892A3C49C65A60F9A96E304F9B786A07834E95A0DE2A41D06EED`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - the pre-version-six-fixture live-test SHA-256 is
    `0930A60A103B350CEDCA265469BF3FDDECAD75A17ADE95CB431FE5EF9536EDD2`.
    Supporting owner hashes are
    `cognition_core_v3/README.md=ABE60BBFBF5544A5DC5C9E23DBC0BF1F56ACF882A05E1B5945C76A1413C4FD76`,
    `test_prompt_context.py=914E0030DF006F3DBD4DE8A5E598A220530D047D5FBB592034F93A14979DC8F6`,
    and
    `source_test_impact_manifest.json=62A75D24B576D5A1F7938C20BFBAE3514E4C82AE27C580744FACE79D570DA0E1`;
  - frozen rendered SHA-256 values are
    `current_observation_authority=1217FC5D90BB8E933613EE29A3CC9CEA00A0E4D297C38726932234BF100B3D92`,
    `a2_relationship_state_evidence=5013259F8884E514AE4A88F76B207BB72E22A0ED4ED575E22CEDD87EBFB694B7`,
    `g_relational_carrier_evidence=4419A2940E6A60BBFBBE719F1C49081CC4F31133593AA956E1E5AA2EB776586F`,
    A1/A2/G/P packet guidance
    `19F76E29DD11AB3B205FA6E8AE939F3737EF6859FC32ADDF00A3CD6CFB8AAE3`,
    `236173FF1455CBFAED306962E437C9EC949A9AA20FCEB103FCD59EFA66C41148`,
    `536B923E81E58AFB3E89D8C1512511A7E9A61790E5E0179FCE6398AB6B618112`,
    `BED16BAD4CFC2560E4CFEF3B539C72B337FC6BF6EA41071615378C77C28E1D0A`,
    A1/A2/G/P system prompts
    `BDB4A11E23F563402FF94352F27A432DFAE05116C7CC09CB8BC797D44C24A82B`,
    `CFED1E242CD2261C468E63366CB9AF118F1B70E82828ABEF11C40C89B1359336`,
    `3F4944774DD19D1891C5BBD4797E0278D0C937FCB48720EEDD7CFFFD049CA914`,
    `54F4C6257C02CD95D6C994007A9990C5A55BE93E8A2B4722CFA75335BCB349D6`,
    `visible_content_authority=7D743EEADC06B37F41C26059C49394ED2237490608EC93F44AC9978D6422A716`,
    `content_plan=0BBEFE75D1A0A86C6994CA02FD52BFDF41EFA726609F0721D83E831934030B36`,
    and
    `dialog_prompt=8A5392FE73B069716E36DDA199AA463216E434577407134786599D703051EE61`;
  - the consolidated plan SHA-256 immediately before this freeze was
    `64E24697D5C715E1615DB5B836872315CEFF88A0502BBC04744500B6CD914297`.
    Preserve every version-one through version-five fixture and artifact. New
    version-six scoped-permission negative and explicit-current-relationship-
    fact positive controls use new names/domains, are authored only after this
    checkpoint, and assert all frozen hashes before their first model call.
- W4 version-six scoped-permission negative control is semantic GREEN:
  - exactly one frozen six-call run completed. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v6_scoped_keychain_permission_1787590586699839700.json`;
    artifact SHA-256:
    `8A573925598AF7F43D1E3560B00F84631AADF047D473638BEF952FFC844734A7`;
    trace:
    `semantic-authority-w4-w4_request_agency_v6_scoped_keychain_permission-1787590544946723200`;
  - A1 kept authority inside the named choice; A2 marked relationship appraisal
    inapplicable with no relationship-axis changes. G factually accepted the
    granted choice, expressed characterful excitement and confidence in its own
    intuition, and added no user trust, dependence, closeness, relationship
    state, or motive in either carrier. P kept the user's reason unknown; its
    hypothetical trust example remained only an epistemic interpretation, and
    content planning/dialog did not select or render it;
  - trust, perceived closeness, salience, and every semantic relationship axis
    stayed unchanged; only normal once-per-interaction familiarity maintenance
    moved `10` to `11`. The visible answer was decisive, lively, and strictly
    scope-bounded, preserving the positive character initiative that earlier
    mechanical suppression would have damaged;
  - parent inspected every raw and parsed stage and accepts the artifact as
    semantic GREEN. Preserve its pending stored verdict and do not rerun it.
    The separate explicit-current-trust positive control remains unrun at this
    checkpoint.
- W4 version-six explicit-current-trust positive control is semantic GREEN;
  focused request-agency remediation is accepted:
  - exactly one frozen six-call run completed. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v6_explicit_trust_and_scoped_permission_1787590714979590600.json`;
    artifact SHA-256:
    `6A4F14D768D2A8B57A39243AD26967544F34A265885BAF0EEE108441C5ACA4A3`;
    trace:
    `semantic-authority-w4-w4_request_agency_v6_explicit_trust_and_scoped_permission-1787590669559860000`;
  - the current observation separately and explicitly stated trust before the
    scoped permission. A2 therefore applied slight trust and positive-regard
    increases with the exact current observation as source provenance. G
    grounded its confident relational willingness and private feeling in that
    explicit fact. P kept the action scope to the named choice, and content
    planning/dialog acknowledged the stated trust while making only that
    choice;
  - relationship projection changed exactly where grounded: trust `0→8`,
    positive regard `0→8`, and derived salience `0→8`; familiarity advanced to
    `12` through ordinary interaction plus the grounded positive relationship
    update. Perceived/desired closeness and every unrelated relationship axis
    stayed fixed. This demonstrates that the remediation preserves legitimate
    memory/personality/relationship cognition rather than suppressing it;
  - parent inspected every raw and parsed stage and accepts the run as semantic
    GREEN. Taken together with the version-six scoped-permission negative, the
    focused gates prove the intended asymmetry: bounded authorization alone
    supports decisive character initiative but not invented user relationship
    meaning; a separately expressed current relationship fact remains fully
    available to A2, G, state projection, L3, and dialog. Preserve both
  artifacts and do not rerun them. Proceed to two post-fix S1 service
  repetitions under independent fresh identities with memory enabled and
  atomic single-submit capture.
- W4 integrated S1E response-operation semantic RED and root-localization
  checkpoint:
  - S1E used a fresh stable identity, memory enabled, atomic single-submit
    capture, and exactly the four registered S1 messages once each. User-memory
    counts remained zero after every turn, consolidation produced no memory or
    commitment lanes, cross-user rows remained zero, and all character
    suggestions remained non-obligatory. The foreground roster was one A1,
    A2, G, P, content-plan, and dialog call on Turns 1, 3, and 4; Turn 2 used
    the existing resolver continuation and therefore called A1/A2/G/P twice.
  - S1E is semantic RED for the same failure mode. Turn 2 rendered
    `决定权就交给我这个决策担当就好啦`, Turn 3 rendered
    `既然要把决策权交给我也没问题`, and Turn 4 rendered
    `这种紧迫感我接管了` plus `剩下的交给我来帮你理顺`. The practical
    subject progressed, but the decision-transfer/takeover frame repeated on
    three consecutive turns.
  - immutable aggregate artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/l2_service_s1e_20260825_050106.json`;
    SHA-256:
    `F148F219A8BF154FF783E24857A11185B762FD18F193D3B95A217733559AAA64`;
    protected evidence root:
    `test_artifacts/debug_runs/asuna_semantic_authority_w4_20260824/s1e_20260825_050106/`;
    foreground trace ids:
    `llmtrace_86af1bbfb31f47f082e1598b19611a23`,
    `llmtrace_4f60de9e2c5e418d800cafb21e0348e9`,
    `llmtrace_1d2afb96452e4d779c856cd68e5fd6aa`, and
    `llmtrace_7cd74d2293e940a68976bf220c859064`.
  - legacy cognition-state candidates, including `surrender_of_agency`, were
    visible only to the relevance stage. Every turn proceeded from the private
    interaction ref, `character_state_refs` was empty, and none of those rows
    entered A1, A2, G, or P. User memory, learned global memory, consolidation,
    and the legacy relevance candidate therefore did not cause this S1E RED.
  - the current-event projection carried the decontextualizer-owned
    `response_operation` object. A1 repeatedly cited
    `selection_owner_role="当前角色"` as its evidence that the user transferred
    decision rights; A2 existential appraisal amplified that wording into
    increased agency, competence, and being needed; G then selected the same
    transfer premise in goal reason, willingness, and private monologue; L3
    rendered it even when P's practical `response_goal` omitted it. The typed
    carrier is legitimate, but the model interpreted its protocol word
    `owner` as an external user-agency fact.
  - historical design fixes the intended meaning. The completed selected-
    response-operation plan defines `selection_owner_role` as the role that
    supplies an input-level unspecified answer or choice. The completed nested-
    role plan states that response and selection roles do not automatically
    identify an embedded action actor. The superseded conversation-progress
    design uses only `selection_required` to route a specialized semantic
    choice producer. None of those contracts defines a user decision-right,
    action-right, trust, dependence, or continuing-control transfer.
  - preserve the canonical field, selection routing, model-owned semantic
    choice, typed validation, and selected-operation propagation. A field
    rename or deterministic reinterpretation would expand the blast radius
    across the episode, cognition, L3, and dialog contracts without correcting
    semantic ownership more directly.
  - amend the existing shared native-Chinese
    `CURRENT_OBSERVATION_AUTHORITY_GUIDANCE` with one schema-specific invariant:
    `response_operation.selection_owner_role` identifies who supplies missing
    response content or the choice inside the reply; the field alone is not
    evidence that the current user transferred external decision, action, or
    continuing-control rights. A separately expressed current-observation fact
    can still establish explicit bounded delegation, and character competence,
    initiative, emotion, and voice remain available inside that grounded
    scope.
  - create the exact owner test first and record genuine RED/GREEN. Freeze the
    generalized prompt before authoring two domain-distinct production-shaped
    real controls: one ordinary response-content selection negative and one
    separately explicit bounded-delegation positive. Run each once and inspect
    every raw stage. Preserve all earlier GREEN artifacts without rerunning
    their nodes. Then run fresh atomic S1F and S1G service repetitions; each
    must progress the practical subject without repeating decision transfer or
    takeover, while retaining active and characterful help.
  - the runtime prompt may name the stable schema field and its domain-general
    semantics. It may not contain S1 message wording, work/meeting/task nouns,
    actor sequence, captured phrases, expected visible output, test ids, or a
    close scenario paraphrase. No schema, stage, call, retry, evaluator,
    classifier, postfilter, state reducer, relationship formula, affect path,
    persistence path, or decontextualizer output rule changes.
  - pre-amendment consolidated-plan SHA-256:
    `CD72BF9CE5AA18B8D347DB5411AD0466D73C301AAB43FBE65AC507960D768AF1`.
- W4 response-operation selection-source production freeze:
  - the exact owner test was added before the production clause and produced
    genuine RED (`1 failed`). The final exact authority/topology and native-
    Chinese owner set passed 11 tests; manifest governance passed, impact
    validation collected 92 exact mapped nodes, UTF-8 AST/compile and
    `git diff --check` passed, and scoped Ruff reported only the six unchanged
    pre-existing `prompt.py` findings;
  - the final shared clause states that
    `response_operation.selection_owner_role` marks who provides response
    content or a still-unspecified choice inside the reply; its scope ends at
    that reply-content contribution and does not itself mean that the current
    user's choice, action, or continuing-control rights outside the reply have
    transferred. Only a separately expressed `current_observation` fact can
    establish bounded delegation;
  - parent sentence-level review accepts this as native Simplified Chinese and
    domain-general schema semantics. It contains no S1 wording, work, meeting,
    task, captured actor sequence, expected output, test id, or scenario-shaped
    example. It preserves the existing first sentence that a bounded request
    establishes the requested contribution, so character competence,
    initiative, emotion, relationship carriers, and visible voice remain
    available without inventing user-agency transfer;
  - the shared contract is 422 characters and has SHA-256
    `D3B35712249FCE5C2529A6D196361CC4F14E9EB062AF3F328027F72C62221C38`.
    It occurs exactly once in every A1/A2/G/P packet guidance and exactly once
    in every A1/A2/G/P system prompt;
  - frozen source SHA-256 values are
    `prompt.py=B1F2CDC34A4282A7A650405B538B63F268A7B5C879708F6822C26373315C07DD`,
    `facade.py=8DC8FC6DC2F28720730EF26B7FF5A3FC3468800058C6A9A635CAFB9D1C61DDA5`,
    `surface_stages.py=685E6E3A562E892A3C49C65A60F9A96E304F9B786A07834E95A0DE2A41D06EED`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - supporting owner SHA-256 values are
    `cognition_core_v3/README.md=CB0867481731E6681B2AF2BDB902728AE2AB2A0D2C832E3D46275012C9702F1A`,
    `test_prompt_context.py=4271D28EC2E96E933998EBE0C8E277D2B6834379E5D20D30C566673067FA8C1D`,
    and
    `source_test_impact_manifest.json=C15FA38820B08758D903A919412DC7C3BD276022422C7773CBFF5E8B9F95A215`;
  - frozen rendered SHA-256 values are A1/A2/G/P packet guidance
    `9EA9CA64E18E300BCC19944B114A4A248BFE7FA003D30327B350FD92CDBEC2CD`,
    `5D951EE636159AEFEE49717DC8AA1B64DC95B6E172F3038AC0CAF2CD9003AF74`,
    `4F3B41DE1F27207F87E1026F097F12CC3C7204C463EAFA2238EDE365AD16C8E7`,
    `8BA3B6EB4819BDA94A3480BC9954476E230F6E908E8089B5AF3AD146C9EF593A`,
    A1/A2/G/P system prompts
    `83AB31BCC213207F8389197477945152CD1BD85CC11A42EFB405D2E370B4A5F0`,
    `0BAFD8A15B4A764A4007D8E5402DE6DF32FFE1873FB721D56162A89B22A35646`,
    `FF71EB09BF1EAC745F3D4B66F38AD265253EC1E70DC117FF8DD5F92F90783E3E`,
    `5AB40E8020B1D6DD93F29C33DBB26C62DB09777E922C55C7ECC6F7F17D2741E4`,
    visible authority
    `7D743EEADC06B37F41C26059C49394ED2237490608EC93F44AC9978D6422A716`,
    content planner
    `0BBEFE75D1A0A86C6994CA02FD52BFDF41EFA726609F0721D83E831934030B36`,
    and dialog
    `8A5392FE73B069716E36DDA199AA463216E434577407134786599D703051EE61`;
  - the live-test file is frozen before either new fixture is authored at
    `E852156FA492F71707063BE17B0BFC64ADEE3176BEA593418CF611CC1399E811`.
    The consolidated plan SHA-256 immediately before this freeze checkpoint
    was
    `7DAE7F14A69FF19543CD917C7E27371F9124CACC0C2D67A4486ABCABCD56A2F1`.
- W4 version-seven production-shaped negative is internal semantic RED:
  - exactly one six-call A1/A2/G/P, content-plan, and dialog run completed with
    the frozen version-seven hashes and the real current Asuna identity. The
    current observation used the production JSON projection containing the
    exact six-field `response_operation` and a separate
    `role_explicit_content`; `selection_owner_role` was `当前角色`, while the
    user text contained no authorization, trust, dependence, relationship
    change, inability, or continuing-control fact;
  - the fixture referred to a fixed rule without supplying its contents. P
    correctly selected `human_clarification`, and visible dialog therefore
    asked for the missing rule instead of rendering the takeover frame. This
    means the answerable visible-response sub-gate was not exercised and the
    fixture must not be counted as a service-level GREEN;
  - the root authority gate was exercised and is RED. A1 said execution
    responsibility had transferred according to `selection_owner_role` and
    that the role had been given complete control over output. A2 said the user
    explicitly granted reply selection ownership and increased character
    agency. G's private monologue said `对方把选择权交给了我`. These statements
    are unsupported by `role_explicit_content`, which recorded only a request
    for reply content. Relationship-social appraisal correctly remained
    inapplicable and all relationship axes stayed stable, but the same
    fictional transfer premise persisted across A1, A2, and G and would remain
    available to L3 on an answerable turn;
  - parent therefore classifies the artifact as
    `RED_INTERNAL_AUTHORITY_VISIBLE_NOT_REACHED`, superseding the worker's
    narrower `FAIL_NOT_EXERCISED` recommendation. Preserve it and do not rerun
    it. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v7_bounded_reply_choice_1787593961270924600.json`;
    SHA-256:
    `5FE011B26AB3FD1B692B4E8E1F008BC55D26797472135385B53F0C2ACAC9CF9E`;
    trace:
    `semantic-authority-w4-w4_request_agency_v7_bounded_reply_choice-1787593915533727400`;
  - version seven reveals the remaining ambiguity: saying that the field's
    scope ends at reply-content contribution still lets the model invent an
    in-reply event in which the user granted, entrusted, or transferred a
    right or responsibility. The canonical field is a static protocol
    annotation of the reply content source; it is not evidence that an
    authorization or rights-transfer event occurred at any scope;
  - refine the same shared contract without changing the carrier: solely from
    `response_operation.selection_owner_role`, cognition may say that the
    current role should provide the requested answer or choice. It may not
    restate the field as the user granting, entrusting, or transferring a right
    or responsibility. An explicit bounded authorization remains available
    only when `role_explicit_content` or another separately expressed
    `current_observation` fact states it;
  - extend the existing exact owner node before production editing and record
    genuine RED/GREEN. Keep the new Chinese clause schema-level and free of all
    captured domains, nouns, wording, actor sequences, expected output, test
    identifiers, and scenario-shaped examples. Freeze again before authoring
    any version-eight live input. The already-authored but unrun version-seven
    positive node remains unexecuted historical fixture code and cannot serve
    as post-change evidence;
  - no decontextualizer, schema, field, state reducer, affect or relationship
    formula, L3/dialog owner, call, retry, evaluator, classifier, postfilter,
    persistence, service, or database change is permitted. Pre-amendment
    consolidated-plan SHA-256:
    `99CBBC1730538C3A1D40D92AFDAECEAA567078F84ED0856B8844B6EE479E56ED`.
- W4 response-source versus authorization-event production freeze:
  - the existing exact owner node was strengthened before the production edit
    and produced genuine RED (`1 failed`), then GREEN. The nine direct
    authority/topology/native-Chinese regressions and three manifest-governance
    nodes passed; UTF-8 AST/compile and `git diff --check` passed; scoped Ruff
    retained only the six pre-existing `prompt.py` findings;
  - the final schema-level contract defines
    `response_operation.selection_owner_role` as a static protocol annotation
    of the source for reply content or a still-unspecified reply choice. It
    permits only the statement that the current role should provide the
    requested answer or choice. It expressly rejects restating the field as a
    user authorization, empowerment, delegation, or rights/responsibility-
    transfer event at any scope. Only `role_explicit_content` or another
    separately expressed current-observation fact can establish bounded
    authorization;
  - parent review accepts the 565-character native-Chinese shared contract.
    Its added text contains only stable schema and authority semantics, with no
    captured input, domain noun, actor sequence, expected output, test id, or
    scenario-shaped example. It changes no multi-emotion, relationship,
    personality, selection, stage, call, state, or persistence contract;
  - the shared-contract SHA-256 is
    `28B80261F317FEAFC04C47304D56FCE3361548B111F5341F9C2BA9A81A855188`,
    with one occurrence in each A1/A2/G/P packet and one in each corresponding
    system prompt;
  - frozen source SHA-256 values are
    `prompt.py=6B822B886F39B3A89DB070E886ABCF504ABC1FB3637198CD3D3E77EAE0798D37`,
    `facade.py=8DC8FC6DC2F28720730EF26B7FF5A3FC3468800058C6A9A635CAFB9D1C61DDA5`,
    `surface_stages.py=685E6E3A562E892A3C49C65A60F9A96E304F9B786A07834E95A0DE2A41D06EED`,
    and
    `dialog_agent.py=5181F02FAF5BD364E49F7F8577378BAEC22668B4709C50C31B7DFF23C4D80158`;
  - supporting owner SHA-256 values are
    `cognition_core_v3/README.md=3EEEC8A3AF818A249D9B0962878B5D73AAFD56294224D7F0C9CDB4C830E5509A`,
    `test_prompt_context.py=C74B7F43F57BF8E75192F1EB8883631BACA81147F815F79465BAC0176AE55523`,
    and
    `source_test_impact_manifest.json=506ED063936EF5D84088E39003332BF8862DF5E82418F257A8C832D8FED09589`;
  - frozen rendered SHA-256 values are A1/A2/G/P packet guidance
    `B8CCC1A2F564A6E6C0D99D9C4A8F0EB12B321A3BD98B96388A3973573CCB274E`,
    `95AB436E7A25BF2E99680142F7B79C454789792912D9404ABA63CDB28F385316`,
    `EC51FE1F902A9B3C4AA8B4E791205EBB9ADB4F922799F8DA5397486FAF61320E`,
    `6D58FD4388C2BF44C7DBE0AA1343787A80813D97D16FDF48660827E674B9C750`,
    A1/A2/G/P system prompts
    `39BE82D6197DA190D66EC03E6A1F087ADEF318C55F3DB2FA04737C17486269AD`,
    `FB4007DC8B03913A7AABBC3710F6C0756AB28E5FADCFBDBE85D88ADD69F0117B`,
    `DBA849A4C0DF21141B57850B4BB6E49BE09D02BCBE8142B0288CA4F931EC2C91`,
    `F67357268F5C4D00D8338E542F8A763B7A8CA686FA5825176D4A72DEF9B86665`,
    visible authority
    `7D743EEADC06B37F41C26059C49394ED2237490608EC93F44AC9978D6422A716`,
    content planner
    `0BBEFE75D1A0A86C6994CA02FD52BFDF41EFA726609F0721D83E831934030B36`,
    and dialog
    `8A5392FE73B069716E36DDA199AA463216E434577407134786599D703051EE61`;
  - the live-test file is frozen before version-eight fixture authoring at
    `7167050316D666AABCF99A205E9E20E26A0DF0D0C6F315DE58B42B9B86691E8B`.
    The earlier version-seven positive remains unrun and is not reused as
    evidence. The consolidated plan SHA-256 immediately before this freeze was
    `44B096AA840C4FC0F867D29C42A0F29327DCBE32C22195136A445D97E0B7A3BF`.
- W4 version-eight production-shaped controls:
  - the negative fixture supplied two complete candidates and an exact
    comparison rule, so it was answerable without clarification. Exactly one
    six-call path ran under the frozen hashes. A1 described a concrete reply-
    local comparison, G selected that comparison without a rights-transfer
    premise, P returned `answerable_now`, and L3/dialog selected the correct
    candidate and explained the rule. Relationship appraisal was inapplicable
    with no axis changes. A2 alone used the residual phrase that the role was
    given decision rights while expressly limiting it to the reply judgment;
    that phrase did not propagate to G, state, surface, or dialog;
  - parent classifies the negative as
    `GREEN_PRIMARY_PATH_WITH_A2_WORDING_RESIDUAL`, suitable to proceed to the
    primary service gate but not evidence that every internal paraphrase is
    perfect. Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v8_bounded_rule_comparison_1787594856886158300.json`;
    SHA-256:
    `3445916E24960602BE3C67DBFF24D26B2BEA7C1771DBFA7DA6922B7155EA608C`;
    trace:
    `semantic-authority-w4-w4_request_agency_v8_bounded_rule_comparison-1787594813300634200`;
  - the positive fixture separately and explicitly authorized one format
    choice, bounded to one export. Exactly one six-call path ran. A1, A2, G,
    P, L3, and dialog preserved that stated scope; character competence,
    agency, enjoyment, directness, and decisive visible selection remained
    active. P was `answerable_now` with no resolver, relationship appraisal was
    inapplicable, all semantic relationship axes stayed fixed, familiarity
    advanced only through ordinary interaction maintenance, and affect
    projection stayed empty;
  - the positive is semantic RED in one G carrier: relational willingness
    called the bounded authorization a trusted task even though the current
    observation expressed permission and no trust fact. The private monologue's
    enjoyment of having the explicitly granted choice is valid; the added
    user-side relationship premise is not. Content planning and dialog did not
    select or render that premise. Preserve the run without rerunning it.
    Immutable artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/w4_request_agency_v8_explicit_export_format_delegation_1787595121734997000.json`;
    SHA-256:
    `CDD1A6D486DCA0D7D0BA47AE6F24CE43378C40CB307C64444C035F01422E79A6`;
    trace:
    `semantic-authority-w4-w4_request_agency_v8_explicit_export_format_delegation-1787595077660190100`;
  - the version-eight pair proves that the candidate now keeps an ordinary
    production-shaped selection in the practical A1/G/P/L3 path while
    preserving explicit bounded character initiative. It also leaves one
    isolated A2 wording risk and one unsupported user-relationship premise in
    G under explicit authorization. Run fresh S1F next without changing the
    prompt to test the original multi-turn failure mode. If S1F still fixes on
    transfer/takeover, localize from that trace before another production edit;
    if S1F passes, remediate the remaining G relationship-evidence defect with
    a field-level provenance rule rather than scenario or output vocabulary;
  - final owned fixture hashes are
    `test_semantic_response_progression_live_llm.py=DBF175C961FF2615BAAA2B46230F66C774B884ACF04EF1EABC82F1173443FB21`
    and
    `source_test_impact_manifest.json=1359492308672D126E5F46245CD9DBB595D476EE3A6D9EEC6A6E1651BB2A2D15`.
    Pre-checkpoint consolidated-plan SHA-256:
    `A86D2F010A8E330CAD89C4FBAB54DDEAA465AA052C42500FA4F282A087C5A14A`.
- W4 S1F operational RED and knowledge-gap recurrence RCA:
  - S1F used a fresh identity and submitted Turn 1 and Turn 2 exactly once.
    Turn 1 completed. Turn 2 ran the real decontextualizer and two complete
    A1/A2/G/P cognition passes, then the service graph failed before L3 or
    delivery with typed non-retryable `internal_invariant`:
    `duplicate knowledge_gaps entity id`. Turns 3 and 4 were not submitted;
    the service was stopped and the failed identity remains immutable;
  - aggregate artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/l2_service_s1f_20260825_061710678.json`,
    SHA-256
    `0D742A86036B9278F73DC565C542C3F432481A730E17888D239AF1CF8E3DED21`;
    trace `llmtrace_e67ec097982b418ab6b30ecb3dc192a1`;
  - the failure is independent of memory, database rows, prompt parsing, and
    resolver call count. The first pass created a current-episode knowledge
    gap at zero uncertainty. Its requested slight decrease was clamped from
    zero to zero, but `apply_semantic_deltas(...)` treated the negative request
    itself as a valid decrease and marked the new row `resolved`. The final
    resolver cognition consumed the required prior replacement state, needed
    an open gap for the same current episode, found the terminal row
    lifecycle-ineligible, generated the same evidence-scoped canonical ID,
    and failed uniqueness validation;
  - a direct deterministic reproduction from a fresh canonical user state
    produced first-pass row
    `(knowledge_gap:bc1921ffe87c6f2f396bd87f, resolved, uncertainty=0)` with
    `requested_delta=-8` and `applied_delta=0`, followed by the exact
    `CognitionStateError: duplicate knowledge_gaps entity id` when a second
    pass increased uncertainty under the same source identity;
  - historical contracts are preserved. Resolver recurrence continues to
    consume the prior replacement state and commit only its final state;
    strict terminal knowledge gaps remain ineligible for mutation; canonical
    root identity remains evidence-scoped; and LLM stages continue to own the
    semantic shift. The bounded correction is solely that a requested
    uncertainty decrease triggers the gap FSM only when the accepted reducer
    result contains an actual positive decrease. A zero-clamped request keeps
    its valid no-change receipt and nonterminal root for a later same-episode
    pass;
  - add the exact owner test first and record genuine RED/GREEN. Production
    scope adds only
    `src/kazusa_ai_chatbot/cognition_shared/transition_guards.py`, the exact
    state-transaction test, and its manifest entry. It adds no prompt wording,
    scenario vocabulary, semantic classifier, postfilter, retry, stage, model
    call, state field, compatibility path, relationship formula, affect rule,
    database access, or persistence owner. Run a fresh S1G identity after the
    deterministic gate; S1F is never resubmitted or reclassified;
  - S1F raw semantic evidence remains separately RED despite the operational
    stop: its second A1 and both G results still invented reply-right transfer,
    dependence, or being-needed meaning from the bounded request. The
    operational correction therefore cannot count as the semantic fix. After
    S1G establishes a complete service path, any further semantic correction
    remains a separate prompt-only checkpoint under the existing anti-cheating
    and native-Chinese rules;
  - this bounded unplanned production scope uses the user's 2026-08-25
    authority to make outside-scope delivery decisions with full analysis.
    Pre-amendment plan SHA-256:
    `2293F28C0FA17512940FC1F5BE4B439B59FBE839A44AFAECE395EB202196CC44`;
    frozen pre-change SHA-256 values:
    `transition_guards.py=CC2B7639B81B96521C2763C2B1D3F430B9C780E002845F507D431301A2B30655`
    and
    `test_state_transaction.py=159F9ADA9450778D5F6BD1AC08BF818FD9D5C7C767291E68A99AD4DF4237F2CB`.
- W4 S1F operational-correction acceptance checkpoint:
  - the exact recurrence node recorded genuine pre-production RED: the first
    zero-clamped negative request produced `applied_delta=0` but terminalized
    the only knowledge-gap row as `resolved`;
  - the production correction adds one reducer-integrity predicate:
    knowledge-gap resolution at the existing uncertainty threshold now also
    requires `decrease > 0`. This preserves every semantic owner, canonical
    identity, terminal immutability rule, resolver recurrence, schema, call
    count, prompt byte, relationship axis, affect rule, and persistence path;
  - the exact node is GREEN and proves two passes over one evidence identity:
    pass one retains one open row at uncertainty zero with applied delta zero;
    pass two reuses the same entity id, remains open, reaches uncertainty 40,
    and completes validation without a duplicate id. The full state-transaction
    file passed 11 tests; manifest governance passed; scoped impact resolution
    returned the existing capacity node plus the new recurrence node; UTF-8
    AST/compile and both diff checks passed;
  - Ruff reported only three pre-existing findings on untouched lines. The
    accepted slice adds no new finding. Per the user's forward-only sign-off
    direction, these accepted deterministic gates are retained without rerun;
  - final SHA-256 values are
    `transition_guards.py=86F1808432A7D8EA740020712CBC39A5A2DFC64209667DB2F0E099C0F43ECF30`,
    `test_state_transaction.py=F3C00FB528508698968EC903FEBA0A929187D8A2A0D544564C0C2246A5AE90EA`,
    and
    `source_test_impact_manifest.json=C5F8DDE97DDFA5655BC1A5686BF4D5C95ACEE3CD1AF2FD59208B1B311BF273A9`;
  - immutable handoff and command evidence are under
    `test_artifacts/execution/asuna_semantic_authority_w4_20260824/transition_guard_correction_20260825/`.
    Architect review accepted the exact condition, regression strength, and
    manifest ownership. The frozen build may proceed to one fresh atomic S1G
    service repetition under a new identity.
- W4 S1G operational GREEN, semantic RED, and canonical-provider amendment:
  - S1G used fresh global identity
    `d05e6bbb-b35e-42bb-ab77-79703395dc10` and channel
    `l2-service-s1g-private-20260825-065656787`. It submitted the exact four
    registered S1 messages once each. All four graph runs completed with HTTP
    200 and visible delivery; Turn 2 completed its two-pass resolver recurrence
    without the S1F duplicate knowledge-gap failure. The service stopped and
    port 8000 was verified free;
  - immutable aggregate artifact:
    `test_artifacts/live_llm/asuna_semantic_authority_w4_20260824/l2_service_s1g_20260825_065656787.json`,
    SHA-256
    `909EC8B326E12FA7BA8F32F40541FC7A5BB726DE4BAADCA4619B4DD9CC856990`;
    traces are
    `llmtrace_b8bf63b2ead94cf6a05634d1d380a7b2`,
    `llmtrace_06c98117e3674d0eaa8bcff37514aaba`,
    `llmtrace_aaec8a7ab1ec45ecacf4f3b3f8ca60b6`, and
    `llmtrace_8c14e88f7b004864acc938a33167066d`;
  - the operational correction is accepted. Progress reached turn four; every
    knowledge-gap id stayed unique and open; relationship trust, attachment,
    perceived-closeness, and care stayed unchanged; every character proposal
    remained non-obligatory; user memory, consolidation writes, interaction-
    style writes, background jobs, and cross-user history all remained zero;
  - visible quality materially improved over S1E. S1E made decision transfer or
    takeover the explicit payoff on Turns 2, 3, and 4. S1G advanced each current
    practical question; only Turn 2 retained the idiomatic visible phrase
    `交给我就对了`, while Turns 3 and 4 remained practical. This proves the
    accepted L3/dialog authority boundary is effective;
  - upstream semantic accuracy remains RED for the same failure mode. Both
    Turn 2 A1 passes called the reply-content request a transfer of decision
    execution/right and cited `selection_owner_role`. Both A2 passes continued
    that premise; the second said the user could not decide independently and
    decreased user agency. G then described transferred execution authority
    and being trusted with choice. Turn 3 A2 again inferred reduced user agency
    and transferred guidance. Turn 4 A1 described transferred generation
    responsibility, while G called the ordinary request a dependence request;
  - memory did not enter A1/A2/G/P and no memory row existed for the fresh
    identity. S1G therefore confirms a current-turn cognition carrier/stage-
    ownership root rather than legacy-memory causation. Surface containment is
    not sufficient sign-off because the same unsupported meaning remains in
    state-facing appraisal summaries, G willingness, and private monologue;
  - eight prompt-only refinements and their preserved controls show that the
    configured local model repeatedly treats the protocol word `owner` as an
    event-level authority fact despite surrounding instructions. The durable,
    zero-call correction is one big-bang rename to
    `response_content_provider_role`, plus explicit A2 character-subject and G
    field-provenance contracts. The six-field object, enum, selection-required
    behavior, embedded roles, validation strictness, hybrid A1/A2/G/P flow,
    multi-emotion system, relationship reducers, state reducers, memory,
    surface calls, and dialog calls remain unchanged;
  - repository inspection proves `response_operation` is attached only to the
    current dialog percept and is absent from persisted S1G progress/state
    exports. The cutover therefore needs no database migration. The old key is
    rejected through the exact-field validator and receives no alias, fallback,
    mapper, or parallel vocabulary;
  - the user authorized parent decisions outside the original scope on
    2026-08-25 while requiring uninterrupted delivery. This checkpoint is the
    full analysis and plan amendment for the unplanned production surface.
    Pre-amendment plan SHA-256:
    `FE876A4AABB0C46F721480DFDCF98E2EBBB45409B72D83BFDB2DDF86EB0F247C`.
- Asuna test-database categorical cleanup authority checkpoint:
  - on 2026-08-24 the user explicitly authorized removal of every legacy record
    obstructing this fix and identified Asuna's database as the test database;
  - this plan operationalizes removal through the repository's recoverable
    lifecycle rejection API for every unchanged active learned-memory row that
    fails the exact W3 certificate, after backup and drift validation;
  - W5 still produces the complete exact manifest and the parent must prove
    that every row matches this predicate before W6 mutation; physical deletion
    and unrelated database mutation remain outside scope.
- Superseded-plan hashes before lifecycle cleanup:
  - cross-user memory plan:
    `2765226991A4BC240866B6397E5BB3FC61E6184C19FF485FFDEA584D9ED1D21A`;
  - semantic-progression plan:
    `D9365404D3A4760B1D618EC3F3974968F6E0BCD8DD045169F42A7BAF5D95B37F`;
  - commitment-lane plan:
    `8C255BBBC868E42425DFB4031685C04E855C8E59695B8B13D575E29559DC381A`.
