# Code Cleanup Plan — 2026-04-21

This plan documents all legacy, unused, and dead code found in the codebase as of
today's scan. Items are split into three tiers:

* **Safe now** — can be removed in any PR without waiting for other changes
* **Pending B1–B5** — must be removed as part of the RAG redesign breaking-change
  groups (see `RAG_REDESIGN.md`)
* **Deferred** — genuine feature debt, tracked here for visibility

---

## Tier 1 — Safe to Remove Now

These items have no dependents in production code and can be cleaned up independently.

### 1.1 Unused import in `dialog_agent.py`

| File | Line | Item |
|------|------|------|
| `src/kazusa_ai_chatbot/agents/dialog_agent.py` | 22 | `from langchain_openai import ChatOpenAI` |

`ChatOpenAI` is never referenced directly in this file; the module uses `get_llm()`
from `utils.py` instead. **Action:** delete the import line.

---

### 1.2 Commented-out test code in `rag/cache.py`

| File | Lines | Block |
|------|-------|-------|
| `src/kazusa_ai_chatbot/rag/cache.py` | 599–610 | `# removed = await cache.invalidate_pattern(...)` and `# miss_after_invalidate = ...` |

These are leftover development probes inside `test_main()`. They are commented out
and no longer needed.  **Action:** delete lines 599–610.

---

### 1.3 Redundant field comments in `state.py`

| File | Lines | Block |
|------|-------|-------|
| `src/kazusa_ai_chatbot/state.py` | 48–59 | `# Input to Persona Supervisor` block with 10 lines of `# xxx: type  # Already requested` |

These comments list fields that already appear earlier in the same `TypedDict` and
add no information. **Action:** delete the entire comment block (lines 48–59 inclusive,
keeping the `# Debug modes` comment at line 61).

---

### 1.4 Stale `# PERSONALITY_PATH removed` comment in `config.py`

| File | Line | Item |
|------|------|------|
| `src/kazusa_ai_chatbot/config.py` | 117 | `# PERSONALITY_PATH removed — character profile is now loaded from MongoDB.` |

The migration note has served its purpose; the code has been using MongoDB for a long
time. **Action:** delete lines 117–118.

---

## Tier 2 — Remove as Part of RAG Redesign Breaking Changes

These items are actively used in the current pipeline but are slated for replacement
by a specific breaking-change group. Do **not** remove them before the corresponding
group is deployed.

### B1 — User Image Switchover

Remove when `user_fact_rag_dispatcher` and `user_fact_rag_finalizer` are replaced by
the persistent `user_profile.user_image` read-path.

| File | Lines | Item |
|------|-------|------|
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag.py` | ~398–481 | `user_fact_rag_dispatcher()` function |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag.py` | ~482–560 | `user_fact_rag_finalizer()` function |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag.py` | `RAGState` | `user_rag_finalized: str` field |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag.py` | ~760–768 | Cache storage block writing `user_rag_finalized` |
| `src/kazusa_ai_chatbot/agents/dialog_agent.py` | prompt | `user_rag_finalized` key in research_facts prompt section |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_consolidator.py` | `_FACTS_HARVESTER_PROMPT` | Reference to `research_facts.user_rag_finalized` in section 2 |

---

### B2 — Character Self-Image Activation

No additional removals beyond exposing `character_image` in the dialog agent prompt
(B2 is purely additive).

---

### B3 — Input-Context RAG Dispatcher Refactor

Remove/rename when `internal_rag_dispatcher` is replaced by `input_context_rag_dispatcher`.

| File | Lines | Item |
|------|-------|------|
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag.py` | ~304–397 | `internal_rag_dispatcher()` — rename to `input_context_rag_dispatcher()` |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_rag.py` | `RAGState` | `internal_rag_results` field — rename to `input_context_results` |
| `src/kazusa_ai_chatbot/agents/dialog_agent.py` | prompt | `internal_rag_results` key |
| `src/kazusa_ai_chatbot/nodes/persona_supervisor2_consolidator.py` | `_FACTS_HARVESTER_PROMPT` | Reference to `research_facts.internal_rag_results` in section 2 |

---

### B5 — Legacy `user_facts` Cache Type Cleanup

Remove when `user_facts` cache type is fully retired (all reads migrated to
`objective_user_facts` + `character_diary`).

| File | Line | Item |
|------|------|------|
| `src/kazusa_ai_chatbot/config.py` | 95 | `"user_facts": int(os.getenv("RAG_CACHE_USER_FACTS_TTL", "1800"))` + `# legacy` comment |
| `src/kazusa_ai_chatbot/rag/cache.py` | `DEFAULT_TTL_SECONDS` | `"user_facts": 1800` entry |
| `src/kazusa_ai_chatbot/db/schemas.py` | `RagCacheIndexDoc` | `"user_facts"` documentation comment |
| `src/kazusa_ai_chatbot/db/users.py` | ~278–390 | `get_user_facts()`, `upsert_user_facts()`, `overwrite_user_facts()`, `enable_user_facts_vector_index()`, `search_users_by_facts()` — legacy shim functions |
| `src/kazusa_ai_chatbot/db/__init__.py` | re-exports | Remove corresponding re-exports and `__all__` entries for the five functions above |
| `tests/test_db.py` | ~650–790 | Test cases covering the five legacy shim functions |

---

## Tier 3 — Deferred / Feature Debt

These are genuine incomplete features flagged by TODO comments. No code to delete —
tracked for future implementation.

| File | Line | TODO | Priority |
|------|------|------|----------|
| `src/kazusa_ai_chatbot/service.py` | ~366 | Extract `scheduled_followups` count from `future_promises` and return it in `ChatResponse` | Medium |
| `src/kazusa_ai_chatbot/service.py` | ~382 | Dispatch `EventRequest` to typed event handlers | Medium |
| `src/kazusa_ai_chatbot/nodes/relevance_agent.py` | ~169 | Pass raw base64 image through the pipeline instead of description-only | Low |

---

## Duplicate Logic — Refactor Candidate (Optional)

`_update_user_image()` (lines 263–354) and `_update_character_image()` (lines 356–423)
in `persona_supervisor2_consolidator.py` implement identical three-tier rolling
mechanics. They could be merged into a single `_update_rolling_image()` helper that
accepts the LLM prompts and DB writer as parameters. This is a refactor, not a removal —
defer until after all B-group breaking changes land so the function signatures stabilise.

---

## Summary

| Tier | Count | Action |
|------|-------|--------|
| Safe now | 4 items | Remove in next cleanup PR |
| Pending B1 | 6 locations | Remove atomically with B1 deployment |
| Pending B3 | 4 locations | Remove atomically with B3 deployment |
| Pending B5 | 12 locations | Remove atomically with B5 deployment |
| Feature debt TODOs | 3 items | Schedule separately |
| Refactor candidate | 1 | Post-B5 |
