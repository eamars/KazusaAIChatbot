allowBuilds:
  # Native shell execution and sandbox bindings require their install hooks.
  '@deepseek-ai/dsh-subprocess-local': true
  koffi: true
  node-pty: true
  # These published JavaScript packages need no installation-time build.
  '@google/genai': false
  protobufjs: false
