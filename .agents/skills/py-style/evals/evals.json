{
  "skill_name": "py-style",
  "evals": [
    {
      "id": 1,
      "prompt": "Review this function for style issues:\n\n```python\nasync def get_user_affinity(global_user_id, db=None):\n    try:\n        if db is None:\n            from kazusa_ai_chatbot.db import get_db\n            db = await get_db()\n        doc = await db.user_profiles.find_one({\"global_user_id\": global_user_id})\n        affinity = doc.get(\"affinity\", 500)\n        logger.info(\"Got affinity %d for user %s\", affinity, global_user_id)\n        return affinity\n    except Exception:\n        return 500\n```",
      "expected_output": "Identifies the violations with constraint IDs: inline import (P-001/N-001), over-broad except Exception (N-002), scattered default 500 via .get() (P-004/N-004), try-except swallowing an internal DB failure (N-003), logging interpolation style (P-008/N-008), direct computed return if present (P-005/N-006), and missing docstring (P-003). Proposes corrected version.",
      "assertions": [
        {
          "name": "flags_inline_import",
          "description": "Response identifies the inline import inside the function body as a P-001/N-001 violation"
        },
        {
          "name": "flags_broad_except",
          "description": "Response flags 'except Exception' as too broad and an N-002 violation"
        },
        {
          "name": "flags_get_default",
          "description": "Response flags doc.get('affinity', 500) as a P-004/N-004 violation -- default should live at write time"
        },
        {
          "name": "flags_swallowed_db_error",
          "description": "Response identifies that catching Exception and returning 500 silences a DB failure (N-003)"
        },
        {
          "name": "flags_missing_docstring",
          "description": "Response flags the missing docstring as a P-003 violation"
        },
        {
          "name": "provides_corrected_code",
          "description": "Response includes a corrected version of the function with all violations fixed"
        }
      ]
    },
    {
      "id": 2,
      "prompt": "Write a function called `classify_message_depth` that takes a user message string, a topic string, and an affinity integer, calls an LLM to decide if the message is SHALLOW or DEEP, and returns a dict with 'depth' and 'reasoning'. The LLM might return broken JSON.",
      "expected_output": "Produces code that: imports at top level, has a full docstring with Args/Returns, wraps only json.loads in try/except json.JSONDecodeError (not the whole LLM call), does not use .get() with defaults on internal state, and does not use bare except.",
      "assertions": [
        {
          "name": "no_inline_imports",
          "description": "All imports are at module level, not inside the function"
        },
        {
          "name": "has_complete_docstring",
          "description": "Function has docstring with purpose, Args section, and Returns section"
        },
        {
          "name": "try_wraps_only_json_parse",
          "description": "The try block contains only the JSON parsing statement, not the entire LLM call chain"
        },
        {
          "name": "except_is_specific",
          "description": "except clause names json.JSONDecodeError or equivalent specific type, not Exception"
        },
        {
          "name": "no_swallowed_llm_call_errors",
          "description": "The LLM ainvoke() call is not wrapped in a try-except that silences failures"
        }
      ]
    },
    {
      "id": 3,
      "prompt": "I have this code and my tech lead said the error handling is wrong. Can you fix it?\n\n```python\nimport asyncio\n\nasync def process_and_save(state):\n    try:\n        user_id = state[\"global_user_id\"]\n        facts = state.get(\"new_facts\", [])\n        affinity_delta = state.get(\"affinity_delta\", 0)\n        \n        from kazusa_ai_chatbot.db import get_db, upsert_user_facts, update_affinity\n        db = await get_db()\n        \n        if facts:\n            await upsert_user_facts(user_id, facts)\n        \n        if affinity_delta != 0:\n            new_affinity = await update_affinity(user_id, affinity_delta)\n        \n        logger.info(\"Saved %d facts, affinity delta %d\", len(facts), affinity_delta)\n        return {\"success\": True}\n    except Exception as e:\n        logger.warning(\"process_and_save failed: %s\", e)\n        return {\"success\": False}\n```",
      "expected_output": "Explains why the tech lead is right using constraint IDs: inline imports (P-001/N-001), try wraps the whole function body instead of the minimum range (P-002), except Exception is too broad (N-002), no docstring (P-003), .get() with defaults on internal state (P-004/N-004), swallowing DB errors hides bugs (N-003), and logging interpolation should use f-strings (P-008/N-008). Provides corrected version where the try-except is removed entirely since there is no external uncertainty that justifies it.",
      "assertions": [
        {
          "name": "moves_imports_to_top",
          "description": "Fixed code moves the inline imports to the module top level"
        },
        {
          "name": "removes_catch_all_try_except",
          "description": "Fixed code removes or significantly narrows the try-except block; internal DB calls are not wrapped"
        },
        {
          "name": "adds_docstring",
          "description": "Fixed code includes a docstring with purpose, Args, and Returns"
        },
        {
          "name": "explains_get_default_issue",
          "description": "Response explains that state.get('new_facts', []) and state.get('affinity_delta', 0) mask missing upstream writes"
        },
        {
          "name": "no_success_false_return",
          "description": "Fixed code does not return a success/failure dict that silences exceptions -- errors should propagate"
        }
      ]
    },
    {
      "id": 4,
      "prompt": "Review this helper for py-style issues:\n\n```python\nfrom typing import Any\n\n\ndef _keyword_text_filter(pattern: str) -> dict[str, Any]:\n    \"\"\"Build the Phase-F regex filter for typed conversation text.\"\"\"\n\n    regex_filter = {\"$regex\": pattern, \"$options\": \"i\"}\n    filter_doc: dict[str, Any] = {\"body_text\": regex_filter}\n    return filter_doc\n```",
      "expected_output": "Flags the Phase-F wording as a P-012/N-013 violation because code comments and docstrings must not mention transient development plan phases. Proposes a stable runtime/domain description such as \"Build a case-insensitive filter for typed conversation text.\" Also flags the helper if appropriate under P-011/N-012.",
      "assertions": [
        {
          "name": "flags_plan_phase_docstring",
          "description": "Response identifies the Phase-F docstring wording as a P-012/N-013 violation"
        },
        {
          "name": "explains_transient_plan_problem",
          "description": "Response explains that plan phases are temporary implementation context and become misleading later"
        },
        {
          "name": "suggests_stable_wording",
          "description": "Response proposes a replacement docstring based on stable runtime or domain behavior"
        }
      ]
    }
  ]
}
